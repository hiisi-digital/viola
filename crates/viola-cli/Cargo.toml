[package]
name = "viola-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Viola host CLI executable. Implementation scheduled for #195."
repository.workspace = true

[[bin]]
name = "viola-cli"
path = "src/main.rs"
test = false

[dependencies]
viola-core = { path = "../viola-core" }

[target.'cfg(unix)'.dependencies]
libc = "0.2"
