//! `viola-bridge-deno` — Deno runtime bridge plugin (scaffold).
//!
//! Implementation lands in #196 (Implement viola-bridge-deno). The
//! prior TS-port stub here referenced descriptor fields that pre-date
//! the v1 ABI shape; it has been removed pending the real bridge
//! design round.
//!
//! This crate currently exposes no public surface. It exists in the
//! workspace so its `Cargo.toml` and crate name remain reserved.
