[package]
name = "viola-test-plugin-fixture"
version = "0.1.0"
edition = "2021"
publish = false
description = "Internal cdylib fixture exercising the v1 plugin ABI for host loader integration tests. Not published."

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
viola-plugin-abi = { path = "../viola-plugin-abi" }
viola-plugin-abi-macros = { path = "../viola-plugin-abi-macros" }
