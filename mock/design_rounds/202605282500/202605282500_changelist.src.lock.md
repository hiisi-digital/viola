# Src CL: NAM v1.2.0 node-kind vocabulary extension

**Round:** 202605282500 **Phase:** SRC (locked) **Doc CL:** locked
`202605282500_changelist.doc.lock.md` **Scope:** `viola-plugin-abi` source
changes for the v1.2.0 kind vocabulary.

## CHANGE: eight `node_kind` constants ADDED (ids 20..=27)

File: `viola/mock/crates/viola-plugin-abi/src/nam.rs`

Appended to the `node_kind` module after `FOREIGN_MOD_ITEM` (19):
`CALL_EXPRESSION` (20), `FIELD_EXPRESSION` (21), `FIELD_DECLARATION` (22),
`PARAMETER` (23), `ENUM_VARIANT` (24), `TYPE_IDENTIFIER` (25), `PRIMITIVE_TYPE`
(26), `ASSOCIATED_TYPE` (27). Append-only; ids 0..=19 unchanged.

Verification:
`grep "CALL_EXPRESSION: USize = USize(20)" viola/mock/crates/viola-plugin-abi/src/nam.rs`
returns one match;
`grep "ASSOCIATED_TYPE: USize = USize(27)" viola/mock/crates/viola-plugin-abi/src/nam.rs`
returns one match.

## CHANGE: const `NamVersion::V1_2_0` ADDED

File: `viola/mock/crates/viola-plugin-abi/src/nam.rs`

```rust
pub const V1_2_0: Self = Self::new(1, 2, 0);
```

Same wire layout as v1.1.0; signals the extended kind vocabulary. The
`nam_file_entries` / `nam_file_nodes` accessors are unchanged (they gate on
`major == 1`).

Verification:
`grep "pub const V1_2_0" viola/mock/crates/viola-plugin-abi/src/nam.rs` returns
one match.

## CHANGE: tests ADDED (`node_kind_ids_are_contiguous_and_distinct`, `nam_version_v1_2_0`)

File: `viola/mock/crates/viola-plugin-abi/src/lib.rs`

The first asserts every `node_kind` id equals its position in the canonical
order (proving distinctness + append-only contiguity, and that the new ids
occupy 20..=27); the second pins `V1_2_0 = (1, 2, 0)`.

Verification: `cargo test -p viola-plugin-abi` passes (20 tests).

## Lock criteria

All CHANGE blocks verify; `cargo test -p viola-plugin-abi` green;
`cargo mock check` green; no `mockspace.toml` severity edits.

## Comparison to deprecated changelists

None.
