**Date:** 2026-05-24
**Phase:** DOC
**Scope:** No `*.md.tmpl` doc changes in this round. The IPC refactor restructures source-only.

# Doc changes

None. The DESIGN.md.tmpl files for viola-deno-runtime, viola-test-plugin-fixture, and viola-test-runner-fixture were already rewritten in PR #14 against the locked-target intent (the `### TypeName` plus prose convention). The refactor in this round implements the source matching those design docs; no doc-side update is required.

The renames in the fixture crates (struct names gaining a `Test` prefix) are internal trampoline type names not surfaced in DESIGN.md.tmpl. The DESIGN docs already speak of "provider implementation" and "lifecycle handler" abstractly; the specific type names that implement those concepts are an implementation detail.

# Out of scope

Future doc churn (#225 design rounds for viola CLI host wiring, #254 hilavitkutin-app conversion, etc.) lands in separate rounds with their own doc CLs.
