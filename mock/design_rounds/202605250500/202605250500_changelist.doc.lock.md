**Date:** 2026-05-25
**Phase:** DOC
**Scope:** Slice 6a doc CL. Adds `## Slice 6a RunLint commutative shape` DESIGN section documenting MAX_LINTS, the COMMUTATIVE flag, and the disjoint-slot-range write strategy. Adds seven BACKLOG entries for the Slice 6b body deferrals.
**Source TOPIC:** `mock/design_rounds/202605250500_topic.viola-254-slice-6a-runlint-design.md`.

# Doc changes

## CHANGE: doc section `## Slice 6a RunLint commutative shape` ADDED to viola-core DESIGN.md.tmpl
File: `mock/crates/viola-core/DESIGN.md.tmpl`
Verification: `grep -q "^## Slice 6a RunLint commutative shape" mock/crates/viola-core/DESIGN.md.tmpl`.

New section documenting Slice 6a: `MAX_LINTS: usize = 32` const cap, `RunLint<L>` carrying `const COMMUTATIVE: Bool = arvo::Bool::TRUE` per the scoping memo's load-bearing claim for parallel fan-out, the disjoint-slot-range write strategy under which RunLint<L> writes a per-L row range of `Column<WuDiagnostic>` rather than the singleton-row convention RunRunner uses. Cross-references Slice 9's parallel fan-out verification audit.

## CHANGE: BACKLOG entry `RunLint body and FFI invocation` ADDED to viola-core/BACKLOG.md.tmpl
File: `mock/crates/viola-core/BACKLOG.md.tmpl`
Verification: `grep -q "RunLint body and FFI invocation" mock/crates/viola-core/BACKLOG.md.tmpl`.

Slice 6a sets the COMMUTATIVE flag and MAX_LINTS const; the body stays `unimplemented!()` until Slice 6b lands the supporting Resources and the FFI invocation.

## CHANGE: BACKLOG entry `Resource<LintSlots> for RunLint slot-to-plugin mapping` ADDED to viola-core/BACKLOG.md.tmpl
File: `mock/crates/viola-core/BACKLOG.md.tmpl`
Verification: `grep -q "Resource<LintSlots> for RunLint" mock/crates/viola-core/BACKLOG.md.tmpl`.

Slice 6b adds `Resource<LintSlots>`: `[arvo::Cap; MAX_LINTS]` mapping slot L to the ExtensionHost host_idx of the lint plugin at that slot, with sentinel for "no plugin at this slot". Populator either extends LoadPlugins Write set or lives in a new tiny IndexLints WU between LoadPlugins and the first RunLint.

## CHANGE: BACKLOG entry `Resource<LintConfigBuffer> for per-slot lint config bytes` ADDED to viola-core/BACKLOG.md.tmpl
File: `mock/crates/viola-core/BACKLOG.md.tmpl`
Verification: `grep -q "Resource<LintConfigBuffer> for per-slot lint config bytes" mock/crates/viola-core/BACKLOG.md.tmpl`.

Slice 6b adds a host-shim-built Resource carrying `[BytesRef; MAX_LINTS]` (one per slot), each pointing into the ViolaCfg arena's bytes. Avoids forcing RunLint<L> to take ViolaCfg in Read (shared dependency would block scheduler parallelisation across the cohort).

## CHANGE: BACKLOG entry `lint_vtable_from_library helper for viola-core invoke` ADDED to viola-core/BACKLOG.md.tmpl
File: `mock/crates/viola-core/BACKLOG.md.tmpl`
Verification: `grep -q "lint_vtable_from_library helper" mock/crates/viola-core/BACKLOG.md.tmpl`.

Slice 6b adds `lint_vtable_from_library(&Library) -> Maybe<&'static LintEvaluateVtable>` parallelling Slice 5b's runner_vtable_from_library. Walks the descriptor's provider table for `PROVIDER_LINT_EVALUATE`.

## CHANGE: BACKLOG entry `MAX_DIAGS_PER_LINT const and per-L row-range projection` ADDED to viola-core/BACKLOG.md.tmpl
File: `mock/crates/viola-core/BACKLOG.md.tmpl`
Verification: `grep -q "MAX_DIAGS_PER_LINT" mock/crates/viola-core/BACKLOG.md.tmpl`.

Slice 6b adds `MAX_DIAGS_PER_LINT` const. Each RunLint<L> writes to slots `[L * MAX_DIAGS_PER_LINT, L * MAX_DIAGS_PER_LINT + MAX_DIAGS_PER_LINT)` of Column<WuDiagnostic>. Disjoint per-L row ranges; parallel writes commute by construction under the COMMUTATIVE flag.

## CHANGE: BACKLOG entry `Host-side LintFinding carrier richer than WuDiagnostic` ADDED to viola-core/BACKLOG.md.tmpl
File: `mock/crates/viola-core/BACKLOG.md.tmpl`
Verification: `grep -q "Host-side LintFinding carrier" mock/crates/viola-core/BACKLOG.md.tmpl`.

Slice 6b reuses WuDiagnostic with Str::default() placeholders for the rich fields until the host-shim long-lived interner lands. Tracks the eventual richer carrier (plugin_id, rule_id, message, path, suggestion as interned Str handles) as a future refinement. Trigger: the Slice 2b host-shim long-lived interner BACKLOG entry shipping.

## CHANGE: BACKLOG entry `Slice 9 parallel fan-out verification` ADDED to viola-core/BACKLOG.md.tmpl
File: `mock/crates/viola-core/BACKLOG.md.tmpl`
Verification: `grep -q "Slice 9 parallel fan-out verification" mock/crates/viola-core/BACKLOG.md.tmpl`.

Per the scoping memo: audit pass against landed hilavitkutin dispatch codegen confirming `COMMUTATIVE = Bool::TRUE` on `RunLint<L>` yields reduce-style parallel fan-out across `RunLint<0>..RunLint<MAX_LINTS-1>`. If it serialises, the design needs revisiting per the cdylib memo's first remaining deferral. Tracked as Slice 9 in the scoping memo.

# Verification target

After the round ships: `cargo check` clean. `cargo mock --lint-only --commit` clean. `MAX_LINTS` exists in source. RunLint<L> carries `const COMMUTATIVE: Bool = arvo::Bool::TRUE`. BACKLOG entries unbackticked.
