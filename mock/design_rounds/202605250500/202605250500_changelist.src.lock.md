**Date:** 2026-05-25 **Phase:** SRC **Scope:** Source changes for Slice 6a. Adds
`MAX_LINTS: usize = 32` const in viola-core/src/resources.rs. Sets
`const COMMUTATIVE: arvo::Bool = arvo::Bool::TRUE` on the `WorkUnit` impl for
`RunLint<L>` in viola-core/src/wus/run_lint.rs. Body stays `unimplemented!()`
per the locked DOC CL; Slice 6b lands the body together with the supporting
Resources. **Source DOC:**
`mock/design_rounds/202605250500_changelist.doc.lock.md`.

# Source changes

## CHANGE: const `MAX_LINTS: usize = 32` ADDED to viola-core resources

File: `mock/crates/viola-core/src/resources.rs` Verification:
`grep -q "pub const MAX_LINTS" mock/crates/viola-core/src/resources.rs`.

Workspace-default cap for the lint-plugin slot count. Pre-1.0; revisable. Sits
alongside `MAX_DISCOVERED_FILES` (4096) and `viola_config::MAX_PLUGINS` (16) as
the third workspace-cap constant viola-core exposes.

## CHANGE: `const COMMUTATIVE: arvo::Bool = arvo::Bool::TRUE` SET on `WorkUnit` impl for `RunLint<L>`

File: `mock/crates/viola-core/src/wus/run_lint.rs` Verification:
`grep -q "const COMMUTATIVE: arvo::Bool = arvo::Bool::TRUE" mock/crates/viola-core/src/wus/run_lint.rs`.

The Slice 1 stub's `WorkUnit` impl carries the default
`const COMMUTATIVE: Bool = Bool::FALSE` from the `WorkUnit` trait declaration.
Slice 6a overrides to `Bool::TRUE` per the scoping memo's load-bearing claim for
parallel fan-out. The Read, Write, Hint, and Ctx<'frame> associated types stay;
the body stays `unimplemented!()`.

# Verification target

- `cargo check -p viola-core` clean.
- `cargo mock --lint-only --commit` clean.
- `MAX_LINTS` exists in source. The `RunLint<L>` `WorkUnit` impl declares
  `const COMMUTATIVE: arvo::Bool = arvo::Bool::TRUE`.
