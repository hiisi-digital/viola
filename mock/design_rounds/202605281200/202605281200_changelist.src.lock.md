# Source changelist: round 202605281200

**Phase:** SRC (living until lock) **Topic:**
`202605281200_topic.abi-v2-overflow-test.md` **Doc CL:**
`202605281200_changelist.doc.lock.md` **Scope:** Add one host-loader test
exercising the v2 `evaluate` overflow contract.

## CHANGE: add `fixture_overflow_reports_internal` test to `host_loader.rs`

File: `mock/crates/viola-core/tests/host_loader.rs`. Add a
`#[test] fn fixture_overflow_reports_internal()` that loads the fixture cdylib,
resolves the lint vtable, and calls `evaluate` with
`out_capacity = arvo::USize(0)` and a zero-length output buffer. Asserts
`status == ExtensionAbiStatus::Internal` and `out_len.0 == 1` (the fixture's
emit count), confirming the plugin reports the would-have-emitted count and
writes nothing on overflow.

Verification:
`grep "ExtensionAbiStatus::Internal" mock/crates/viola-core/tests/host_loader.rs`
returns at least one match; `cargo test -p viola-core` passes after
`cargo build -p viola-test-plugin-fixture`.

## Lock criteria

The CHANGE block landed; `cargo test -p viola-core` passes; `cargo mock check`
green.

## Comparison to deprecated changelists

None.
