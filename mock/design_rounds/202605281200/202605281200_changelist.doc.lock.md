# Doc changelist: round 202605281200

**Phase:** DOC (living until lock) **Topic:**
`202605281200_topic.abi-v2-overflow-test.md` **Scope:** Add a host-loader test
covering the v2 `evaluate` overflow contract. Source-only round (one test
added); no design-doc edits.

## What ships

The existing `host_loader.rs` integration test covers the non-overflow (`Ok`)
path of `LintEvaluateVtable.evaluate`. The v2 overflow contract (return
`AbiStatus::Internal`, set `*out_len` to the would-have-emitted count, write
nothing past capacity) is unverified. This round adds a second test that drives
the overflow path through the real fixture cdylib.

## CHANGE: add overflow-path test to `host_loader.rs`

File: `viola/mock/crates/viola-core/tests/host_loader.rs`. A new `#[test]` loads
the fixture cdylib, resolves the lint vtable, and calls `evaluate` with
`out_capacity = 0`. It asserts the status is `ExtensionAbiStatus::Internal` and
`*out_len` equals the fixture's emit count (`1`).

Verification:
`grep "Internal" viola/mock/crates/viola-core/tests/host_loader.rs` returns at
least one match; `cargo test -p viola-core` passes (with the fixture cdylib
built).

## Lock criteria

The CHANGE block landed; `cargo test -p viola-core` passes; `cargo mock check`
green.

## Comparison to deprecated changelists

None.
