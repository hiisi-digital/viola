# Doc changelist: round 202605281800

**Phase:** DOC (living until lock) **Topic:**
`202605281800_topic.project-scope-abi-test-fixes.md` **Scope:** Strengthen the
`IndexBatch` and `LintEvaluateProjectIndexVtable` layout tests to assert field
offsets. Source-only.

## What ships

A pre-merge review found the two layout tests check only total size and discard
their `offset_of!` results, so they cannot catch field reordering. This round
makes them assert offsets.

## CHANGE: assert field offsets in the project-scope layout tests

File: `viola/mock/crates/viola-plugin-abi/src/lib.rs`. In
`index_batch_layout_pointer_plus_three_usize`, replace the discarded
`offset_of!` calls with `assert_eq!` on each offset (`entries` at 0; `capacity`
/ `len` / `needed` at successive offsets computed from
`size_of::<*mut c_void>()` and `size_of::<arvo::USize>()`). In
`project_index_vtable_is_two_words`, add `assert_eq!` that `index_phase` is at
offset 0 and `evaluate_phase` at one function-pointer width.

Verification:
`grep "assert_eq!(offset_of" viola/mock/crates/viola-plugin-abi/src/lib.rs`
returns at least two matches; `cargo test -p viola-plugin-abi` passes.

## Lock criteria

The CHANGE block landed; `cargo test -p viola-plugin-abi` passes;
`cargo mock check` green.

## Comparison to deprecated changelists

None.
