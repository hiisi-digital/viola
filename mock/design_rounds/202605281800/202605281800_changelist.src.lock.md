# Source changelist: round 202605281800

**Phase:** SRC (living until lock) **Topic:**
`202605281800_topic.project-scope-abi-test-fixes.md` **Doc CL:**
`202605281800_changelist.doc.lock.md` **Scope:** Strengthen the two
project-scope layout tests in `viola-plugin-abi/src/lib.rs` to assert field
offsets.

## CHANGE: assert field offsets in `index_batch_layout_*` and `project_index_vtable_*`

File: `mock/crates/viola-plugin-abi/src/lib.rs`. Replace discarded `offset_of!`
calls in `index_batch_layout_pointer_plus_three_usize` with `assert_eq!` on each
field offset (`entries == 0`; `capacity`, `len`, `needed` at offsets derived
from `size_of::<*mut c_void>()` + multiples of `size_of::<arvo::USize>()`). In
`project_index_vtable_is_two_words`, add `assert_eq!` that `index_phase` is at
offset 0 and `evaluate_phase` at one function-pointer width.

Verification:
`grep "assert_eq!(offset_of" mock/crates/viola-plugin-abi/src/lib.rs` returns at
least two matches; `cargo test -p viola-plugin-abi` passes.

## Lock criteria

The CHANGE block applied; `cargo test -p viola-plugin-abi` passes;
`cargo mock check` green.

## Comparison to deprecated changelists

None.
