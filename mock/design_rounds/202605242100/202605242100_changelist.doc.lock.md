**Date:** 2026-05-24 **Phase:** DOC **Scope:** Docs-only round closing the Slice
2a audit-trail gap. Lands the missing `### TypeName` sections in
`viola-config/DESIGN.md.tmpl` and `viola-core/DESIGN.md.tmpl`, the BACKLOG entry
for `InvocationSurface`, and removes three SHAME entries whose types now have
real DESIGN coverage. **Source TOPIC:**
`mock/design_rounds/202605242100_topic.viola-254-slice-2-design-sections.md`.

# Doc changes

## `mock/crates/viola-config/DESIGN.md.tmpl`

Seven new `### TypeName` sections covering the Slice 2a additions. The existing
`### ViolaConfig` section gains a paragraph cross-referencing the new owned-form
bridge.

## CHANGE: doc section `### ViolaConfigOwned` ADDED

File: `mock/crates/viola-config/DESIGN.md.tmpl` Verification:
`grep -q "^### ViolaConfigOwned" mock/crates/viola-config/DESIGN.md.tmpl`.

## CHANGE: doc section `### ViolaCfg` ADDED

File: `mock/crates/viola-config/DESIGN.md.tmpl` Verification:
`grep -q "^### ViolaCfg" mock/crates/viola-config/DESIGN.md.tmpl`.

## CHANGE: doc section `### ConfigArena` ADDED

File: `mock/crates/viola-config/DESIGN.md.tmpl` Verification:
`grep -q "^### ConfigArena" mock/crates/viola-config/DESIGN.md.tmpl`.

## CHANGE: doc section `### ConfigBytes` ADDED

File: `mock/crates/viola-config/DESIGN.md.tmpl` Verification:
`grep -q "^### ConfigBytes" mock/crates/viola-config/DESIGN.md.tmpl`.

## CHANGE: doc section `### CONFIG_MAX_BYTES` ADDED

File: `mock/crates/viola-config/DESIGN.md.tmpl` Verification:
`grep -q "^### CONFIG_MAX_BYTES" mock/crates/viola-config/DESIGN.md.tmpl`.

## CHANGE: doc section `### MAX_PLUGINS` ADDED

File: `mock/crates/viola-config/DESIGN.md.tmpl` Verification:
`grep -q "^### MAX_PLUGINS" mock/crates/viola-config/DESIGN.md.tmpl`.

## CHANGE: doc section `### MAX_GATES` ADDED

File: `mock/crates/viola-config/DESIGN.md.tmpl` Verification:
`grep -q "^### MAX_GATES" mock/crates/viola-config/DESIGN.md.tmpl`.

## CHANGE: doc section `### MAX_RULES` ADDED

File: `mock/crates/viola-config/DESIGN.md.tmpl` Verification:
`grep -q "^### MAX_RULES" mock/crates/viola-config/DESIGN.md.tmpl`.

## CHANGE: doc section `### MAX_PARTIAL_RULES` ADDED

File: `mock/crates/viola-config/DESIGN.md.tmpl` Verification:
`grep -q "^### MAX_PARTIAL_RULES" mock/crates/viola-config/DESIGN.md.tmpl`.

## CHANGE: doc section `### ARENA_BYTES` ADDED

File: `mock/crates/viola-config/DESIGN.md.tmpl` Verification:
`grep -q "^### ARENA_BYTES" mock/crates/viola-config/DESIGN.md.tmpl`.

## `mock/crates/viola-core/DESIGN.md.tmpl`

Six revised or new sections for the Slice 2a viola-core changes.

## CHANGE: doc section `### Workspace` REVISED

File: `mock/crates/viola-core/DESIGN.md.tmpl` Verification:
`grep -q "^### Workspace" mock/crates/viola-core/DESIGN.md.tmpl`.

## CHANGE: doc section `### CiState` REVISED

File: `mock/crates/viola-core/DESIGN.md.tmpl` Verification:
`grep -q "^### CiState" mock/crates/viola-core/DESIGN.md.tmpl`.

## CHANGE: doc section `### AgentKind` ADDED

File: `mock/crates/viola-core/DESIGN.md.tmpl` Verification:
`grep -q "^### AgentKind" mock/crates/viola-core/DESIGN.md.tmpl`.

## CHANGE: doc section `### LoadConfig` REVISED

File: `mock/crates/viola-core/DESIGN.md.tmpl` Verification:
`grep -q "^### LoadConfig" mock/crates/viola-core/DESIGN.md.tmpl`.

## CHANGE: doc section `### LoadPlugins` REVISED

File: `mock/crates/viola-core/DESIGN.md.tmpl` Verification:
`grep -q "^### LoadPlugins" mock/crates/viola-core/DESIGN.md.tmpl`.

## CHANGE: doc section `### DiscoverFiles` REVISED

File: `mock/crates/viola-core/DESIGN.md.tmpl` Verification:
`grep -q "^### DiscoverFiles" mock/crates/viola-core/DESIGN.md.tmpl`.

## `mock/crates/viola-core/BACKLOG.md.tmpl`

## CHANGE: BACKLOG entry `InvocationSurface` ADDED

File: `mock/crates/viola-core/BACKLOG.md.tmpl` Verification:
`grep -q "InvocationSurface" mock/crates/viola-core/BACKLOG.md.tmpl`.

## `mock/crates/viola-config/SHAME.md.tmpl`

Remove the three entries whose types now have real DESIGN sections:
`ConfigBytes`, `ConfigArena`, `ViolaConfigOwned`. The four placeholder
owned-record entries (`PluginEntryOwned`, `GateOwned`, `RuleOwned`,
`PartialRuleOwned`) stay until their body slices land real fields. `ViolaCfg` is
an alias of `ViolaConfigOwned`; its entry was never landed and does not need
removal.

## CHANGE: SHAME entry `## ConfigBytes` REMOVED

File: `mock/crates/viola-config/SHAME.md.tmpl` Verification:
`! grep -q "^## ConfigBytes$" mock/crates/viola-config/SHAME.md.tmpl`.

## CHANGE: SHAME entry `## ConfigArena` REMOVED

File: `mock/crates/viola-config/SHAME.md.tmpl` Verification:
`! grep -q "^## ConfigArena$" mock/crates/viola-config/SHAME.md.tmpl`.

## CHANGE: SHAME entry `## ViolaConfigOwned` REMOVED

File: `mock/crates/viola-config/SHAME.md.tmpl` Verification:
`! grep -q "^## ViolaConfigOwned$" mock/crates/viola-config/SHAME.md.tmpl`.

# Out of scope

- Sections for `PluginEntryOwned`, `GateOwned`, `RuleOwned`, `PartialRuleOwned`.
  The TOPIC reasons through why: documenting a ZST placeholder is documentation
  theatre; the body slice that flips the type to fields-bearing authors the real
  section.
- Slice 2b body. Separate round.
- Rust source changes. None in this round.

# Verification target

- `cargo check -p viola-config` and `cargo check -p viola-core` stay green (no
  source changes; the doc-doc lint does not exercise build).
- `cargo mock --lint-only --commit` passes; the seven `undocumented-type`
  violations from Slice 2a clear for the three covered types plus the alias.
- `design-doc-source-mismatch` lint accepts every backticked type name in the
  updated DESIGN.md.tmpl files against current source state.
