# Source changelist: round 202605281400

**Phase:** SRC (living until lock)
**Topic:** `202605281400_topic.nam-v1-1-0-design-doc.md`
**Doc CL:** `202605281400_changelist.doc.lock.md`
**Scope:** No source changes. This round updates `viola-plugin-abi/DESIGN.md.tmpl` only; the NAM v1.1.0 source shipped under round 202605281300.

## No source changes

The NAM v1.1.0 ABI surface (`NamVersion::V1_1_0`, `NamNode`, `NamFileEntry.nodes`, `nam_file_nodes`, the widened `nam_file_entries` gate, `node_kind`) shipped under round 202605281300's SRC CL and is locked. This round touches only the design-doc template, handled under its DOC CL.

Verification: `git diff --name-only <round-base>..HEAD -- 'mock/crates/**/*.rs'` returns no entries for this round's commits.

## Lock criteria

DOC CL locked; no `.rs` source touched in this round; `cargo mock check` green.

## Comparison to deprecated changelists

None.
