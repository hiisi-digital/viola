# Doc changelist: round 202605281400

**Phase:** DOC (living until lock) **Topic:**
`202605281400_topic.nam-v1-1-0-design-doc.md` **Scope:** Document the NAM v1.1.0
surface in `viola-plugin-abi/DESIGN.md.tmpl`. No source changes (shipped in
round 202605281300).

## What ships

The NAM section currently documents only `NamPayload` and `NamVersion`. This
round adds the per-file entry, the v1.1.0 node array, the canonical id table,
and the walk accessors, all of which exist in source as of round 202605281300.

## CHANGE: `viola-plugin-abi/DESIGN.md.tmpl` NAM section extended for v1.1.0

File: `viola/mock/crates/viola-plugin-abi/DESIGN.md.tmpl`. Under
`## NAM carriers`, add subsections describing `NamFileEntry` (path, language,
source, and the v1.1.0 `nodes` carrier), `NamNode` (the flat AST node with the
slice-length sentinel for `parent` / `first_child`), the `node_kind` canonical
Rust id table, and the `nam_file_entries` / `nam_file_nodes` accessors. Note
that the v1.x `NamFileEntry` layout is shared across versions and that the host
serialiser that fills `nodes` ships with the rust-native runner.

Verification: `grep "NamNode" viola/mock/crates/viola-plugin-abi/DESIGN.md.tmpl`
returns at least one match;
`grep "nam_file_nodes" viola/mock/crates/viola-plugin-abi/DESIGN.md.tmpl`
returns at least one match; every backticked type added exists in
`viola-plugin-abi` source.

## Lock criteria

The CHANGE block landed; the design-doc-source-mismatch lint passes (every
backticked type exists in source); `cargo mock check` green. No SRC CL change.

## Comparison to deprecated changelists

None.
