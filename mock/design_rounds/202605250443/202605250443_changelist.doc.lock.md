**Date:** 2026-05-25
**Phase:** DOC
**Scope:** Slice 8b.2 doc CL. Adds DESIGN.md.tmpl `## Slice 8b.2 scheduler build chain` section. Revises the Slice 8a-deferred BACKLOG entry for the scheduler wiring to mark "Shipped in Slice 8b.2".
**Source TOPIC:** `mock/design_rounds/202605250443_topic.viola-254-slice-8b2-scheduler-wiring.md`.

# Doc changes

## CHANGE: doc section `## Slice 8b.2 scheduler build chain` ADDED to viola-core DESIGN.md.tmpl
File: `mock/crates/viola-core/DESIGN.md.tmpl`
Verification: `grep -q "^## Slice 8b.2 scheduler build chain" mock/crates/viola-core/DESIGN.md.tmpl`.

New DESIGN section documenting the full scheduler-build chain shipped in viola-cli's unix-main: 12 Resources, 4 Columns, 5 unit-WUs plus the 32-instance `RunLint<N>` cohort registered via a local `register_lints!` declarative macro. Names the 12 Resources (the prior Slice 8a-deferred BACKLOG entry mentioned 9; the complete set per the WU AccessSet union is 12 — the three additions are `Resource<ConfigBytes>`, `Resource<RunSurface>`, `Resource<ViolaCfg>`). Notes the `EmitWriter: Send + Sync` supertrait promotion that the chain requires (the Resource<DiagnosticSink<W>> Sync proof needs the supertrait). Notes the `#![recursion_limit = "1024"]` raise required for the deep cons-list `ContainsAll` typestate proofs. Documents that runtime executability waits on `hilavitkutin::Scheduler::run`'s body landing (currently `unimplemented!()` per Pass 6 signature commit; Pass 7+8 of the runtime megaround ship the body), and that Slice 8b.2's lock criterion is the AccessSet contracts compiling at `.build()` time.

## CHANGE: BACKLOG entry `viola-cli scheduler wiring and concrete EmitWriter impl` REVISED in viola-core/BACKLOG.md.tmpl
File: `mock/crates/viola-core/BACKLOG.md.tmpl`
Verification: `grep -q "Shipped in Slice 8b.2" mock/crates/viola-core/BACKLOG.md.tmpl`.

The entry's body shifts from forward-looking ("Slice 8b.2 ships the full chain ...") to retrospective ("Shipped in Slice 8b.2: ..."). Body retains the chain enumeration for audit-trail value but the framing matches the shipped state. Per `no-legacy-shims-pre-1.0` and the workspace BACKLOG convention, this is an in-place revision, not a removal — the entry stays as the durable record of the deferral that just closed.

## CHANGE: BACKLOG entry `ConfigBytes Default impl` ADDED to viola-config/BACKLOG.md.tmpl
File: `mock/crates/viola-config/BACKLOG.md.tmpl`
Verification: `grep -q "ConfigBytes Default impl" mock/crates/viola-config/BACKLOG.md.tmpl`.

New retrospective entry marking the small Slice 8b.2 addition: `impl Default for ConfigBytes` that returns an empty buffer (`bytes: [0u8; CONFIG_MAX_BYTES]`, `len: arvo::Cap(arvo::USize::ZERO)`). Required for viola-cli's scheduler-builder to `Resource::new(ConfigBytes::default())` without inline bare-numeric literals. Marked "Shipped in Slice 8b.2" for audit-trail completeness.

# Verification target

After the round ships: `cargo build -p viola-cli` clean (from viola root). `cargo test --workspace` clean. The three `## CHANGE:` verification predicates above pass. `cargo mock --lint-only --commit` clean.
