**Date:** 2026-05-25 **Phase:** DOC **Scope:** Slice 8b.2 reviewer-fix DOC CL.
Adds a new BACKLOG entry for the Outcome-handling follow-up. Refines two prose
occurrences of "substrate" in DESIGN.md.tmpl per the workspace vocabulary rule.
**Source TOPIC:**
`mock/design_rounds/202605250508_topic.viola-254-slice-8b2-reviewer-fixes.md`.

# Doc changes

## CHANGE: BACKLOG entry `viola-cli Scheduler::run Outcome handling` ADDED to viola-core/BACKLOG.md.tmpl

File: `mock/crates/viola-core/BACKLOG.md.tmpl` Verification:
`grep -q "viola-cli Scheduler::run Outcome handling" mock/crates/viola-core/BACKLOG.md.tmpl`.

New BACKLOG entry naming the follow-up that wires `Scheduler::run`'s `Cfg::Out`
return value to a concrete viola-cli exit code (EXIT_OK on Ok, EXIT_PLUGIN on
Err). Sequencing: the follow-up lands AFTER the engine body ships (hilavitkutin
Pass 7+8), not before, because the concrete return shape is the engine's choice.
Closes reviewer's finding 1 by naming a concrete deferral target instead of
hand-waving "intentional for the transitional state".

## CHANGE: vocabulary `substrate` -> `foundations` / `foundations type` in viola-core/DESIGN.md.tmpl

File: `mock/crates/viola-core/DESIGN.md.tmpl` Verification:
`! grep -n 'substrate' mock/crates/viola-core/DESIGN.md.tmpl | grep -v '^[0-9]*:[ ]*$'`
returns no matches in the `## Slice 8b.2 scheduler build chain` section (the
Slice 8a section's existing occurrences are pre-existing and out of scope for
this round).

Two prose occurrences land in the Slice 8b.2 section that the workspace
vocabulary rule flags. Replacement: "substrate type" -> "foundations type" (one
occurrence in the Slice 8b.1 ExtensionHost re-export drop subsection); "the
substrate `hilavitkutin_extensions::ExtensionHost`" -> "the foundations type
`hilavitkutin_extensions::ExtensionHost`" (one occurrence in the twelve-Resource
list). Closes reviewer's finding 3.
