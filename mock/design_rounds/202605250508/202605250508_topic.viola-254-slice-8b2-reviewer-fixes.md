# Topic: viola-254 Slice 8b.2 reviewer fixes

**Date:** 2026-05-25
**Phase:** TOPIC
**Scope:** viola-cli, viola-core, viola-config
**Source topics:** Post-review fixes for PR #53 (Slice 8b.2). Branch carries the Slice 8b.2 round which has just closed; this round opens to land the reviewer's three findings without leaving them as follow-ups.

## Current state

Slice 8b.2 just closed and shipped the full scheduler-build chain. PR #53 opened. `pr-reviewer-senior` dispatched and returned three findings:

1. [correctness] `viola-cli/src/main.rs:243` — `let _ = scheduler.run();` followed by `EXIT_OK`. Once `Scheduler::run`'s body lands (engine Pass 7+8), the `let _ =` will silently discard the returned `Cfg::Out` `Outcome`. The current comment hand-waves "intentional for the transitional state" but does not name a concrete follow-up.

2. [drift] `viola-config/src/lib.rs` — `impl Default for ConfigBytes` couples viola-config to a scheduler-wiring concern. A named constructor (`empty()` / `new()`) would carry the call shape without claiming `ConfigBytes` has a semantically meaningful default value.

3. [rules] `viola-core/DESIGN.md.tmpl` — two occurrences of the banned word "substrate" (per workspace vocabulary rule). Sweep to "foundations" / "foundations type".

## Changes proposed

Three CHANGE blocks. None of them changes the visible engineering of Slice 8b.2; they refine the post-review surface per reviewer feedback.

1. `viola-core/BACKLOG.md.tmpl`: new entry `## viola-cli Scheduler::run Outcome handling` naming the follow-up that wires the Outcome to a concrete exit code once the engine body lands. Sequencing note: follow-up lands after Pass 7+8, not before.

2. `viola-cli/src/main.rs` comment update: replace the hand-wavy comment block before `let _ = scheduler.run();` with a concrete reference to the new BACKLOG entry.

3. `viola-config/src/lib.rs`: add `pub fn ConfigBytes::empty()` returning the empty buffer; `impl Default for ConfigBytes` keeps but delegates to `Self::empty()`. The named constructor IS the primary constructor; Default is the thin trait shim for builder patterns.

4. `viola-cli/src/main.rs`: update the chain call from `Resource::new(ConfigBytes::default())` to `Resource::new(ConfigBytes::empty())`.

5. `viola-core/DESIGN.md.tmpl`: replace two occurrences of "substrate" with "foundations" / "foundations type" in the `## Slice 8b.2 scheduler build chain` section.

## Lock criteria

- `cargo build -p viola-cli` clean.
- `cargo test --workspace` clean.
- `grep substrate mock/crates/viola-core/DESIGN.md.tmpl` returns no matches in the new section.
- `grep "pub fn empty" mock/crates/viola-config/src/lib.rs` finds the new constructor.
- `grep "ConfigBytes::empty()" mock/crates/viola-cli/src/main.rs` finds the updated call site.
- `grep "Scheduler::run Outcome handling" mock/crates/viola-core/BACKLOG.md.tmpl` finds the new entry.
