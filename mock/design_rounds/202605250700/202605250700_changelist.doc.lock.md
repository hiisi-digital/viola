**Date:** 2026-05-25
**Phase:** DOC
**Scope:** Slice 7a doc CL. Adds `## Slice 7a EmitDiagnostics design` DESIGN section documenting five design tensions + Slice 7b's chosen paths in a table. Revises `### DiagnosticSink` section to document the non-ZST placeholder (deliberately not yet a carrier). Adds six BACKLOG entries for Slice 7b deferrals.
**Source TOPIC:** `mock/design_rounds/202605250700_topic.viola-254-slice-7a-emit-diagnostics-design.md`.

# Doc changes

## CHANGE: doc section `## Slice 7a EmitDiagnostics design` ADDED to viola-core DESIGN.md.tmpl
File: `mock/crates/viola-core/DESIGN.md.tmpl`
Verification: `grep -q "^## Slice 7a EmitDiagnostics design" mock/crates/viola-core/DESIGN.md.tmpl`.

New section documenting Slice 7a's design ratification. Names the five tensions (DiagnosticSink shape, EmitWriter trait surface, sort strategy under Str::default placeholders, surface choice, disjoint per-L row-range gap handling). Names Slice 7b's chosen paths in a table (trait-driven DiagnosticSink generic carrier, minimal EmitWriter trait with return type open question, host-side approximation sort with original-slot-index tiebreaker for stability, hardcode human-readable, Resource<DiagnosticCounts> for gap handling with a RunLint Write extension flagged as load-bearing for the Slice 9 audit).

## CHANGE: doc section `### DiagnosticSink` REVISED in viola-core DESIGN.md.tmpl
File: `mock/crates/viola-core/DESIGN.md.tmpl`
Verification: `grep -q "non-ZST placeholder" mock/crates/viola-core/DESIGN.md.tmpl`.

The Slice 1 ZST placeholder `### DiagnosticSink` section flips to documenting the Slice 7a non-ZST placeholder. Vocabulary: non-ZST placeholder (the type holds a transient counter field for the lone purpose of being non-ZST), not "carrier" (the carrier shape lands in Slice 7b). The placeholder field is non-`pub` so it does not become a deletion-victim backwards-compat surface between 7a and 7b.

## CHANGE: BACKLOG entry `EmitDiagnostics body and emit loop` ADDED to viola-core/BACKLOG.md.tmpl
File: `mock/crates/viola-core/BACKLOG.md.tmpl`
Verification: `grep -q "EmitDiagnostics body and emit loop" mock/crates/viola-core/BACKLOG.md.tmpl`.

Slice 7a ships the non-ZST flip; the body stays unimplemented. Slice 7b lands the body proper.

## CHANGE: BACKLOG entry `EmitWriter trait surface for viola-core` ADDED to viola-core/BACKLOG.md.tmpl
File: `mock/crates/viola-core/BACKLOG.md.tmpl`
Verification: `grep -q "EmitWriter trait surface" mock/crates/viola-core/BACKLOG.md.tmpl && grep -q "return type" mock/crates/viola-core/BACKLOG.md.tmpl`.

Slice 7b adds a minimal `EmitWriter` trait (write_str, write_bytes, flush) in viola-core. **Open question for Slice 7b's CL to settle**: return type. Unit-return is simpler but loses I/O failure visibility; `notko::Outcome<()>` return surfaces failure to the caller for explicit handling. Slice 7b's topic+DOC CL picks a path with rationale; the design call is not collapsed to unit-return in 7a.

## CHANGE: BACKLOG entry `DiagnosticSink generic carrier flip` ADDED to viola-core/BACKLOG.md.tmpl
File: `mock/crates/viola-core/BACKLOG.md.tmpl`
Verification: `grep -q "DiagnosticSink generic carrier flip" mock/crates/viola-core/BACKLOG.md.tmpl`.

Slice 7b flips DiagnosticSink from the Slice 7a non-ZST placeholder to `DiagnosticSink<W: EmitWriter>` with private `writer: UnsafeCell<W>` field. Same four-invariant `unsafe impl Sync` pattern as host-shim Resources.

## CHANGE: BACKLOG entry `Resource<DiagnosticCounts> for per-L populated counts` ADDED to viola-core/BACKLOG.md.tmpl
File: `mock/crates/viola-core/BACKLOG.md.tmpl`
Verification: `grep -q "Resource<DiagnosticCounts>" mock/crates/viola-core/BACKLOG.md.tmpl && grep -q "Slice 9" mock/crates/viola-core/BACKLOG.md.tmpl`.

Slice 7b adds `Resource<DiagnosticCounts>`: `[arvo::USize; MAX_LINTS]` mapping slot L to the count of populated `WuDiagnostic` records. **RunLint's Write set extends to include this Resource** (a load-bearing AccessSet change to the already-closed Slice 6b RunLint WU body); the Slice 9 parallel fan-out verification audit needs to re-check COMMUTATIVE-flag commutativity against the extended Write set. Per-L writes to `DiagnosticCounts[L]` for distinct L commute trivially (disjoint slot indexes), so the audit is expected to confirm; tracked explicitly so the assumption is not silent.

## CHANGE: BACKLOG entry `Host-side WuDiagnostic sort approximation` ADDED to viola-core/BACKLOG.md.tmpl
File: `mock/crates/viola-core/BACKLOG.md.tmpl`
Verification: `grep -q "Host-side WuDiagnostic sort approximation" mock/crates/viola-core/BACKLOG.md.tmpl`.

Slice 7b sorts by `(source, severity, range.start_line, range.start_col, original_slot_index)`. The trailing `original_slot_index` tiebreaker is load-bearing for stability across runs against the same input: WuDiagnostic records carry Str::default() placeholders for path / plugin_id / message, so the leading sort keys can collide on duplicate diagnostics that differ only by interned-field content; the slot-index tiebreaker pins the canonical order until the LintFinding carrier upgrade restores ABI §10's canonical sort. Documented deviation; trigger for canonical restoration is the Slice 2b host-shim long-lived interner BACKLOG entry shipping.

## CHANGE: BACKLOG entry `EmitDiagnostics surface choice hardcoded` ADDED to viola-core/BACKLOG.md.tmpl
File: `mock/crates/viola-core/BACKLOG.md.tmpl`
Verification: `grep -q "EmitDiagnostics surface choice hardcoded" mock/crates/viola-core/BACKLOG.md.tmpl`.

Slice 7b hardcodes the human-readable emit format. JSON / LSP defer to a future slice that introduces `Resource<RunSurface>` (already on the Slice 6a BACKLOG) and branches the emit format on the surface variant.

# Verification target

After the round ships: `cargo check` clean. `cargo mock --lint-only --commit` clean. `DiagnosticSink` exists as a non-ZST struct with a non-`pub` placeholder field.
