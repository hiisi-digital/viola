# Source changelist: round 202605281300

**Phase:** SRC (living until lock) **Topic:**
`202605281300_topic.nam-v1-1-0-schema.md` **Doc CL:**
`202605281300_changelist.doc.lock.md` **Scope:** Apply the NAM v1.1.0 ABI
surface in `viola-plugin-abi` (`src/nam.rs` + `src/lib.rs`).

## CHANGE: const `NamVersion::V1_1_0` ADDED

File: `mock/crates/viola-plugin-abi/src/nam.rs`. Add
`pub const V1_1_0: Self = Self::new(1, 1, 0);` to the `NamVersion` impl.

Verification:
`grep "V1_1_0: Self = Self::new(1, 1, 0)" mock/crates/viola-plugin-abi/src/nam.rs`
returns one match.

## CHANGE: struct `NamNode` ADDED

File: `mock/crates/viola-plugin-abi/src/nam.rs`. Add
`#[repr(C)] #[derive(Copy, Clone)] pub struct NamNode { kind, parent, first_child, start_byte, end_byte, start_row, end_row }`,
all `arvo::USize`, with the slice-length sentinel documented and
`unsafe impl Send`/`Sync`.

Verification:
`grep "pub struct NamNode" mock/crates/viola-plugin-abi/src/nam.rs` returns one
match.

## CHANGE: field `NamFileEntry.nodes` ADDED

File: `mock/crates/viola-plugin-abi/src/nam.rs`. Add
`pub nodes: crate::BytesRef` as the final `NamFileEntry` field.

Verification:
`grep "pub nodes: crate::BytesRef" mock/crates/viola-plugin-abi/src/nam.rs`
returns one match.

## CHANGE: fn `nam_file_nodes` ADDED

File: `mock/crates/viola-plugin-abi/src/nam.rs`. Add
`pub fn nam_file_nodes<'a>(entry: &'a NamFileEntry) -> Option<&'a [NamNode]>`:
`None` on null `nodes.data`, else `Some(slice)`
(`count = nodes.len.0 / size_of::<NamNode>()`).

Verification:
`grep "pub fn nam_file_nodes" mock/crates/viola-plugin-abi/src/nam.rs` returns
one match.

## CHANGE: fn `nam_file_entries` gate WIDENED FROM `!= V1_0_0` TO `major != 1`

File: `mock/crates/viola-plugin-abi/src/nam.rs`. Replace the
`payload.version != NamVersion::V1_0_0` check with `payload.version.major != 1`.
Update the doc comment for v1.x layout stability.

Verification: `grep "major != 1" mock/crates/viola-plugin-abi/src/nam.rs`
returns at least one match;
`! grep "!= NamVersion::V1_0_0" mock/crates/viola-plugin-abi/src/nam.rs`.

## CHANGE: module `node_kind` ADDED

File: `mock/crates/viola-plugin-abi/src/nam.rs`. Add `pub mod node_kind` with
`pub const` `arvo::USize` ids (`UNKNOWN = 0` plus common Rust structural kinds
including `LINE_COMMENT`, `BLOCK_COMMENT`, `VISIBILITY_MODIFIER`, the item
kinds).

Verification: `grep "pub mod node_kind" mock/crates/viola-plugin-abi/src/nam.rs`
returns one match;
`grep "pub const LINE_COMMENT" mock/crates/viola-plugin-abi/src/nam.rs` returns
one match.

## CHANGE: `lib.rs` re-exports + tests updated

File: `mock/crates/viola-plugin-abi/src/lib.rs`. Add `NamNode`,
`nam_file_nodes`, `node_kind` to the `pub use nam::{...}` list. Update the
layout test and the `NamFileEntry { ... }` literals to include
`nodes: BytesRef::EMPTY`. Add `nam_v1_1_0_is_one_one_zero`, `nam_file_nodes`
null/populated tests, and a `V1_1_0` walk test.

Verification: `grep "NamNode" mock/crates/viola-plugin-abi/src/lib.rs` returns
at least one match; `cargo test -p viola-plugin-abi` passes.

## Lock criteria

All six CHANGE blocks applied and verified; `cargo test -p viola-plugin-abi`
passes; `cargo mock check` green.

## Comparison to deprecated changelists

None.
