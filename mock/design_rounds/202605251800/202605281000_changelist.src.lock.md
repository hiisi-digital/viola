# Source changelist: round 202605281000

**Phase:** SRC (living until lock) **Topic:**
`202605281000_topic.abi-v2-lint-evaluate.md` **Doc CL:**
`202605281000_changelist.doc.lock.md` **Scope:** Apply the ABI v2
host-owned-buffer change across the four crates.

## CHANGE: `LintEvaluateVtable.evaluate` FROM `out_batch: *mut DiagnosticBatch` TO `out_entries: *mut Diagnostic, out_capacity, out_len`

File: `mock/crates/viola-plugin-abi/src/vtable.rs`. Replace the `evaluate`
function-pointer parameter list's final `out_batch: *mut DiagnosticBatch` with
`out_entries: *mut Diagnostic`, `out_capacity: arvo::USize`,
`out_len: *mut arvo::USize`. Change `use crate::diagnostic::DiagnosticBatch;` to
`use crate::diagnostic::Diagnostic;`. Update the `LintEvaluateVtable` doc
comment to name `viola.lint.evaluate.v2` and state the overflow contract.

Verification:
`grep "out_entries: \*mut Diagnostic" mock/crates/viola-plugin-abi/src/vtable.rs`
returns one match;
`! grep "DiagnosticBatch" mock/crates/viola-plugin-abi/src/vtable.rs`.

## CHANGE: `DiagnosticBatch` struct REMOVED FROM `diagnostic.rs`

File: `mock/crates/viola-plugin-abi/src/diagnostic.rs`. Delete the
`#[repr(C)] pub struct DiagnosticBatch { entries, len }` plus its
`unsafe impl Send`/`unsafe impl Sync`. Rewrite the trailing batch-ownership doc
paragraph to describe the host-owned output buffer (host allocates, plugin
writes through up to capacity).

Verification:
`! grep "DiagnosticBatch" mock/crates/viola-plugin-abi/src/diagnostic.rs`.

## CHANGE: `PROVIDER_LINT_EVALUATE` FROM `viola.lint.evaluate.v1` TO `viola.lint.evaluate.v2`

File: `mock/crates/viola-plugin-abi/src/lib.rs`. Change the
`ProviderId::from_name(...)` argument. Constant name unchanged.

Verification:
`grep "viola.lint.evaluate.v2" mock/crates/viola-plugin-abi/src/lib.rs` returns
one match; `! grep "evaluate.v1" mock/crates/viola-plugin-abi/src/lib.rs`.

## CHANGE: `DiagnosticBatch` re-export REMOVED FROM `viola-plugin-abi/lib.rs`

File: `mock/crates/viola-plugin-abi/src/lib.rs`. Drop `DiagnosticBatch` from the
`pub use diagnostic::{...}` list and from the module doc bullet.

Verification:
`! grep "DiagnosticBatch" mock/crates/viola-plugin-abi/src/lib.rs`.

## CHANGE: `RunLint<L>::execute` host call FROM `DiagnosticBatch` view TO host-owned `MaybeUninit` buffer

File: `mock/crates/viola-core/src/wus/run_lint.rs`. Drop the `DiagnosticBatch`
import, import `Diagnostic`. Replace the `let mut batch = DiagnosticBatch {...}`
plus call plus projection with: a
`MaybeUninit<[Diagnostic; MAX_DIAGS_PER_LINT]>` buffer, `out_len: arvo::USize`,
a call passing `buf.as_mut_ptr() as *mut Diagnostic`,
`USize(MAX_DIAGS_PER_LINT)`, `&mut out_len`. On `Ok` or `Internal`, project
`min(out_len, MAX_DIAGS_PER_LINT)` initialised entries into the per-L row range;
other statuses emit the failure record as before.

Verification: `grep "out_len" mock/crates/viola-core/src/wus/run_lint.rs`
returns at least one match;
`! grep "DiagnosticBatch" mock/crates/viola-core/src/wus/run_lint.rs`.

## CHANGE: `DiagnosticBatch` re-export REMOVED FROM `viola-core/lib.rs`

File: `mock/crates/viola-core/src/lib.rs`. Drop `DiagnosticBatch` from the
`pub use viola_plugin_abi::{...}` list.

Verification: `! grep "DiagnosticBatch" mock/crates/viola-core/src/lib.rs`.

## CHANGE: `host_loader.rs` test invocation FROM `DiagnosticBatch` TO host buffer

File: `mock/crates/viola-core/tests/host_loader.rs`. Drop `DiagnosticBatch`
import. Replace the `let mut batch = DiagnosticBatch {...}` and
`(vt.evaluate)(...)` call with a host-owned `[Diagnostic; N]` buffer (via
`MaybeUninit`), `out_len`, the v2 call, and assertions on
`ExtensionAbiStatus::Ok` plus the written `out_len`.

Verification:
`! grep "DiagnosticBatch" mock/crates/viola-core/tests/host_loader.rs`;
`grep "out_len" mock/crates/viola-core/tests/host_loader.rs` returns at least
one match.

## CHANGE: test-plugin fixture `evaluate` FROM writing `*out_batch` TO writing host buffer

File: `mock/crates/viola-test-plugin-fixture/src/lib.rs`. Change the `evaluate`
trampoline signature to the v2 shape. Copy the fixture's static diagnostics into
`out_entries` up to `out_capacity`, set `*out_len`, return `AbiStatus::Internal`
if the emit count exceeds `out_capacity` else `Ok`. Drop the `DiagnosticBatch`
import.

Verification:
`! grep "DiagnosticBatch" mock/crates/viola-test-plugin-fixture/src/lib.rs`;
`grep "out_entries" mock/crates/viola-test-plugin-fixture/src/lib.rs` returns at
least one match.

## CHANGE: deno-runtime `lint_evaluate` FROM writing `*out_batch` TO writing host buffer

File: `mock/crates/viola-deno-runtime/src/worker.rs` (+ `src/lib.rs` if the
vtable literal needs the import adjusted). Change the `lint_evaluate` trampoline
signature to the v2 shape. Write into `out_entries` up to `out_capacity`, set
`*out_len`, return `AbiStatus::Internal` on overflow. Drop the `DiagnosticBatch`
import.

Verification:
`! grep "DiagnosticBatch" mock/crates/viola-deno-runtime/src/worker.rs`;
`grep "out_entries\|out_capacity" mock/crates/viola-deno-runtime/src/worker.rs`
returns at least one match.

## Lock criteria

All nine CHANGE blocks applied and verified;
`cargo test -p viola-plugin-abi -p viola-core` passes; the fixture and
deno-runtime crates compile; `cargo mock check` green.

## Comparison to deprecated changelists

None.
