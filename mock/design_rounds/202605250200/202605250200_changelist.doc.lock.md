**Date:** 2026-05-25 **Phase:** DOC **Scope:** Slice 4 doc CL. Lands
DESIGN.md.tmpl sections for the new `DiscoveredFilePaths` Resource, the
`FileInfo` ZST flip + `FileKind` enum, and the `DiscoverFiles` body shape. Lands
BACKLOG entries for the deferred filesystem walker, include / exclude pattern
matching, FileKind classification refinement, FileInfo size hints,
Workspace-relative diagnostic context, and runtime test-Ctx fixture. AccessSet
narrows to match body usage exactly; future refinements extend at their point of
consumption. **Source TOPIC:**
`mock/design_rounds/202605250200_topic.viola-254-slice-4-discover-files-body.md`.

# Doc changes

## CHANGE: doc section `### DiscoveredFilePaths` ADDED to viola-core DESIGN.md.tmpl

File: `mock/crates/viola-core/DESIGN.md.tmpl` Verification:
`grep -q "^### \`DiscoveredFilePaths\`" mock/crates/viola-core/DESIGN.md.tmpl`.

New section under a `## Slice 4 file-discovery shapes` heading. Documents the
host-shim-populated input Resource shape, the four-invariant `unsafe impl Sync`
contract, the `pub unsafe fn push` host-shim seam with `# Safety` clause, the
safe `paths_len` and `path_at` read accessors.

## CHANGE: doc section `### MAX_DISCOVERED_FILES` ADDED to viola-core DESIGN.md.tmpl

File: `mock/crates/viola-core/DESIGN.md.tmpl` Verification:
`grep -q "^### \`MAX_DISCOVERED_FILES\`" mock/crates/viola-core/DESIGN.md.tmpl`.

Const cap for the `DiscoveredFilePaths` Resource. Workspace-default `4096`.
Pre-1.0; revisable.

## CHANGE: doc section `### FileInfo` REVISED in viola-core DESIGN.md.tmpl

File: `mock/crates/viola-core/DESIGN.md.tmpl` Verification:
`grep -q "kind: FileKind" mock/crates/viola-core/DESIGN.md.tmpl`.

The Slice 1 ZST placeholder section flips to a fields-bearing two-field shape.
Documents the `path` and `kind` fields. The variant table for `FileKind` uses
un-backticked variant names per the Slice 2b `WuDiagnosticSource` precedent.

## CHANGE: doc section `### FileKind` ADDED to viola-core DESIGN.md.tmpl

File: `mock/crates/viola-core/DESIGN.md.tmpl` Verification:
`grep -q "^### \`FileKind\`" mock/crates/viola-core/DESIGN.md.tmpl`.

Closed-vocabulary three-variant enum: Regular, Symlink, Other. Per
`vocabulary.md`'s closed-enum-as-spec pattern. Slice 4 constructs only the
Regular variant until the host shim supplies kind info.

## CHANGE: doc section `### DiscoverFiles` REVISED in viola-core DESIGN.md.tmpl

File: `mock/crates/viola-core/DESIGN.md.tmpl` Verification:
`grep -q "Resource<DiscoveredFilePaths>" mock/crates/viola-core/DESIGN.md.tmpl`.

The Slice 2a `### DiscoverFiles` section gets a Slice 4 update naming the body.
Documents the AccessSet refinement (narrowed to match body usage: Read holds
only `Resource<DiscoveredFilePaths>`; Write holds only `Column<FileInfo>`), the
projection loop, the local-counter pattern for column-length tracking, the
`unreachable!()` assertion on the contractually-impossible `Maybe::Isnt` arm.

## CHANGE: BACKLOG entry `Filesystem walker for no_std host` ADDED to viola-core/BACKLOG.md.tmpl

File: `mock/crates/viola-core/BACKLOG.md.tmpl` Verification:
`grep -q "Filesystem walker for no_std host" mock/crates/viola-core/BACKLOG.md.tmpl`.

Names the Path A alternative. Tracks the hilavitkutin-fs stack-side helper as
the eventual right shape.

## CHANGE: BACKLOG entry `Include exclude pattern matching for DiscoverFiles` ADDED to viola-core/BACKLOG.md.tmpl

File: `mock/crates/viola-core/BACKLOG.md.tmpl` Verification:
`grep -q "Include exclude pattern matching for DiscoverFiles" mock/crates/viola-core/BACKLOG.md.tmpl`.

The viola-config round designing `IncludePattern`/`ExcludePattern` lands first;
then DiscoverFiles extends Read to include `Resource<ViolaCfg>`, extends Write
to include `Column<WuDiagnostic>` for invalid-pattern errors, and adds the
matcher.

## CHANGE: BACKLOG entry `FileKind classification beyond Regular` ADDED to viola-core/BACKLOG.md.tmpl

File: `mock/crates/viola-core/BACKLOG.md.tmpl` Verification:
`grep -q "FileKind classification beyond Regular" mock/crates/viola-core/BACKLOG.md.tmpl`.

Slice 4 constructs only `FileKind::Regular`. Refinement extends
`Resource<DiscoveredFilePaths>` to carry per-path kind classification or pairs
with a sibling Resource.

## CHANGE: BACKLOG entry `FileInfo size hints` ADDED to viola-core/BACKLOG.md.tmpl

File: `mock/crates/viola-core/BACKLOG.md.tmpl` Verification:
`grep -q "FileInfo size hints" mock/crates/viola-core/BACKLOG.md.tmpl`.

Future Slice 6 (`RunLint<L>`) may want file-size hints. The exact shape (third
field, per-Kind payload, sibling Column) waits on RunLint specifics.

## CHANGE: BACKLOG entry `Workspace-relative diagnostic context for DiscoverFiles` ADDED to viola-core/BACKLOG.md.tmpl

File: `mock/crates/viola-core/BACKLOG.md.tmpl` Verification:
`grep -q "Workspace-relative diagnostic context for DiscoverFiles" mock/crates/viola-core/BACKLOG.md.tmpl`.

When a future Slice extends DiscoverFiles to emit diagnostics, the WU may want
workspace-relative path framing. That refinement extends Read to include
`Resource<Workspace>`. Tracked as the consumer-slice trigger for that AccessSet
extension.

## CHANGE: BACKLOG entry `Runtime test-Ctx fixture for DiscoverFiles` ADDED to viola-core/BACKLOG.md.tmpl

File: `mock/crates/viola-core/BACKLOG.md.tmpl` Verification:
`grep -q "Runtime test-Ctx fixture for DiscoverFiles" mock/crates/viola-core/BACKLOG.md.tmpl`.

Slice 4 ships a compile-time AccessSet-shape witness test; runtime projection
tests defer per the shared fixture roadmap.

# Verification target

After the round ships: `cargo check` clean. `cargo mock --lint-only --commit`
clean. Every backticked Slice 4 type name in DESIGN.md.tmpl exists in source.
BACKLOG entries UNBACKTICKED.
