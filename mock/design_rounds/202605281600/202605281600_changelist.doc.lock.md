# Doc changelist: round 202605281600

**Phase:** DOC (living until lock)
**Topic:** `202605281600_topic.project-scope-abi.md`
**Scope:** Project-scope lint ABI surface in `viola-plugin-abi`: `IndexBatch`, `MAX_INDEX_ENTRIES`, `LintEvaluateProjectIndexVtable`, `PROVIDER_LINT_EVALUATE_PROJECT`. No host dispatch (lands with the future project-scoped host WorkUnit).

## What ships

Decision 3 (Option ii): two-phase index-then-evaluate dispatch for cross-file lints. This slice ships the ABI both the bucket-3 cdylibs (#619) and the future host WorkUnit compile against.

## CHANGE: struct `IndexBatch` ADDED

File: `viola/mock/crates/viola-plugin-abi/src/vtable.rs`. Add `#[repr(C)] #[derive(Copy, Clone)] pub struct IndexBatch { entries: *mut c_void, capacity: arvo::USize, len: arvo::USize, needed: arvo::USize }` with the opacity + overflow contract documented and `unsafe impl Send`/`Sync`.

Verification: `grep "pub struct IndexBatch" viola/mock/crates/viola-plugin-abi/src/vtable.rs` returns one match; `grep "pub needed: arvo::USize" viola/mock/crates/viola-plugin-abi/src/vtable.rs` returns one match.

## CHANGE: const `MAX_INDEX_ENTRIES` ADDED

File: `viola/mock/crates/viola-plugin-abi/src/vtable.rs`. Add `pub const MAX_INDEX_ENTRIES: arvo::USize = arvo::USize(1 << 20);` with a doc naming it the host's default pre-allocation cap.

Verification: `grep "pub const MAX_INDEX_ENTRIES" viola/mock/crates/viola-plugin-abi/src/vtable.rs` returns one match.

## CHANGE: struct `LintEvaluateProjectIndexVtable` ADDED

File: `viola/mock/crates/viola-plugin-abi/src/vtable.rs`. Add `#[repr(C)] #[derive(Copy, Clone)] pub struct LintEvaluateProjectIndexVtable` with `index_phase` and `evaluate_phase` function pointers per the topic, and `unsafe impl Send`/`Sync`. Update the module doc to list the new provider -> vtable mapping.

Verification: `grep "pub struct LintEvaluateProjectIndexVtable" viola/mock/crates/viola-plugin-abi/src/vtable.rs` returns one match; `grep "index_phase" viola/mock/crates/viola-plugin-abi/src/vtable.rs` returns at least one match.

## CHANGE: const `PROVIDER_LINT_EVALUATE_PROJECT` ADDED

File: `viola/mock/crates/viola-plugin-abi/src/lib.rs`. Add `pub const PROVIDER_LINT_EVALUATE_PROJECT: ProviderId = ProviderId::from_name("viola.lint.evaluate-project.v1");`. Re-export `IndexBatch`, `MAX_INDEX_ENTRIES`, `LintEvaluateProjectIndexVtable` from the `vtable` module. Update the crate module doc's provider list.

Verification: `grep "viola.lint.evaluate-project.v1" viola/mock/crates/viola-plugin-abi/src/lib.rs` returns one match; `grep "LintEvaluateProjectIndexVtable" viola/mock/crates/viola-plugin-abi/src/lib.rs` returns at least one match.

## CHANGE: tests ADDED for the new provider + vtable

File: `viola/mock/crates/viola-plugin-abi/src/lib.rs`. Add tests: `PROVIDER_LINT_EVALUATE_PROJECT` distinct from the other three providers; `LintEvaluateProjectIndexVtable` is two function pointers wide (`size_of` == two words); `IndexBatch` layout (four fields, pointer + three `USize`).

Verification: `cargo test -p viola-plugin-abi` passes.

## Lock criteria

All five CHANGE blocks landed; verification commands return expected counts; `cargo test -p viola-plugin-abi` passes; `cargo mock check` green.

## Comparison to deprecated changelists

None.
