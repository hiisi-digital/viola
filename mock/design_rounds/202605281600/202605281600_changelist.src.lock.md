# Source changelist: round 202605281600

**Phase:** SRC (living until lock) **Topic:**
`202605281600_topic.project-scope-abi.md` **Doc CL:**
`202605281600_changelist.doc.lock.md` **Scope:** Apply the project-scope lint
ABI surface in `viola-plugin-abi` (`src/vtable.rs` + `src/lib.rs`).

## CHANGE: struct `IndexBatch` ADDED

File: `mock/crates/viola-plugin-abi/src/vtable.rs`. Add
`#[repr(C)] #[derive(Copy, Clone)] pub struct IndexBatch { entries: *mut c_void, capacity: arvo::USize, len: arvo::USize, needed: arvo::USize }` +
`unsafe impl Send`/`Sync`, with the opacity and overflow contract in the doc.

Verification:
`grep "pub struct IndexBatch" mock/crates/viola-plugin-abi/src/vtable.rs`
returns one match;
`grep "pub needed: arvo::USize" mock/crates/viola-plugin-abi/src/vtable.rs`
returns one match.

## CHANGE: const `MAX_INDEX_ENTRIES` ADDED

File: `mock/crates/viola-plugin-abi/src/vtable.rs`. Add
`pub const MAX_INDEX_ENTRIES: arvo::USize = arvo::USize(1 << 20);`.

Verification:
`grep "pub const MAX_INDEX_ENTRIES" mock/crates/viola-plugin-abi/src/vtable.rs`
returns one match.

## CHANGE: struct `LintEvaluateProjectIndexVtable` ADDED

File: `mock/crates/viola-plugin-abi/src/vtable.rs`. Add the `#[repr(C)]`
two-pointer vtable (`index_phase`, `evaluate_phase`) +
`unsafe impl Send`/`Sync`. Update the module doc provider list.

Verification:
`grep "pub struct LintEvaluateProjectIndexVtable" mock/crates/viola-plugin-abi/src/vtable.rs`
returns one match;
`grep "index_phase\|evaluate_phase" mock/crates/viola-plugin-abi/src/vtable.rs`
returns at least two matches.

## CHANGE: const `PROVIDER_LINT_EVALUATE_PROJECT` + re-exports ADDED

File: `mock/crates/viola-plugin-abi/src/lib.rs`. Add the provider const
(`viola.lint.evaluate-project.v1`); add `IndexBatch`, `MAX_INDEX_ENTRIES`,
`LintEvaluateProjectIndexVtable` to the `pub use vtable::{...}` list; update the
crate module-doc provider list.

Verification:
`grep "viola.lint.evaluate-project.v1" mock/crates/viola-plugin-abi/src/lib.rs`
returns one match.

## CHANGE: tests ADDED

File: `mock/crates/viola-plugin-abi/src/lib.rs`. Add the provider-distinctness
test, the two-function-pointer-width test for `LintEvaluateProjectIndexVtable`,
and the `IndexBatch` layout test.

Verification: `cargo test -p viola-plugin-abi` passes.

## Lock criteria

All five CHANGE blocks applied and verified; `cargo test -p viola-plugin-abi`
passes; `cargo mock check` green.

## Comparison to deprecated changelists

None.
