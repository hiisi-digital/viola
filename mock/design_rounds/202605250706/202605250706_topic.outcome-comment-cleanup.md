# Topic: outcome handling slice comment cleanup

**Date:** 2026-05-25
**Phase:** TOPIC
**Scope:** viola-cli/src/main.rs single-line stale-comment cleanup. Round 2 on the viola-cli scheduler outcome handling branch.

## Current state

Round 1 (202605250659) shipped the `match scheduler.run() -> exit code` body. Reviewer caught a comment-block hygiene issue: when the Edit replaced the multi-line comment, the first line of the old comment (`// \`Scheduler::run\` returns \`Cfg::Out\`, a generic associated type`) was anchored as a context line in the replacement string by accident and stayed in the file. The result is two consecutive `// \`Scheduler::run\` returns \`Cfg::Out\`...` lines at lines 231-232, with the first line's clause dangling.

## Changes proposed

Delete the stale line 231 so the comment block opens cleanly with the new wording at what becomes the new line 231.

## Lock criteria

- The duplicated comment line is removed.
- `cargo build -p viola-cli` clean.
