**Date:** 2026-05-25 **Phase:** SRC **Scope:** R2 comment cleanup. Removes one
stale comment line from viola-cli/src/main.rs. **Source TOPIC:**
`mock/design_rounds/202605250706_topic.outcome-comment-cleanup.md`. **Source
DOC:** `mock/design_rounds/202605250706_changelist.doc.lock.md`.

# Source changes

## CHANGE: drop stale duplicate comment line in viola-cli/src/main.rs

File: `mock/crates/viola-cli/src/main.rs` Verification:
`! grep -B 1 'Scheduler::run. returns .Cfg::Out., a generic associated type' mock/crates/viola-cli/src/main.rs`.

The previous round's Edit anchored a context line during the replacement,
leaving `//`Scheduler::run`returns`Cfg::Out`, a generic associated type`
orphaned above the new comment block that opens with
`//`Scheduler::run`returns`Cfg::Out`. For`DefaultRunCfg`this`. This change
deletes the stale orphan so the comment block opens cleanly with the new
wording.

# Lock criteria

- The `## CHANGE:` verification predicate passes.
- `cargo build -p viola-cli` clean.
