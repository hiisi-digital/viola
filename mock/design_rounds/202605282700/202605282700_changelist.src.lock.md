# Src CL: NAM v1.3.0 identifier node-kind

**Round:** 202605282700 **Phase:** SRC **Doc CL:** locked
`202605282700_changelist.doc.lock.md` **Scope:** `viola-plugin-abi` source
changes for the v1.3.0 kind vocabulary.

## CHANGE: `node_kind::IDENTIFIER` const ADDED (id 28)

File: `viola/mock/crates/viola-plugin-abi/src/nam.rs`

Appended to the `node_kind` module after `ASSOCIATED_TYPE` (27): `IDENTIFIER`
(28). Append-only; ids 0..=27 unchanged. Names the name-token leaf of an item,
function, field, variant, or binding (read via the node source byte span),
distinct from `TYPE_IDENTIFIER` (25) which names a type.

Verification:
`grep "IDENTIFIER: USize = USize(28)" viola/mock/crates/viola-plugin-abi/src/nam.rs`
returns one match.

## CHANGE: const `NamVersion::V1_3_0` ADDED

File: `viola/mock/crates/viola-plugin-abi/src/nam.rs`

```rust
pub const V1_3_0: Self = Self::new(1, 3, 0);
```

Same wire layout as v1.2.0; signals the extended kind vocabulary. The
`nam_file_entries` / `nam_file_nodes` accessors are unchanged (they gate on
`major == 1`).

Verification:
`grep "pub const V1_3_0" viola/mock/crates/viola-plugin-abi/src/nam.rs` returns
one match.

## CHANGE: tests UPDATED + ADDED (`node_kind_ids_are_contiguous_and_distinct`, `nam_version_v1_3_0`)

File: `viola/mock/crates/viola-plugin-abi/src/lib.rs`

`node_kind_ids_are_contiguous_and_distinct` gains `IDENTIFIER.0` at position 28
in the id array (the position-equality loop then proves contiguity /
distinctness for the new id), with an added `assert_eq!(IDENTIFIER.0, 28)`. A
new `nam_version_v1_3_0` test pins `V1_3_0 = (1, 3, 0)` (mirrors
`nam_version_v1_2_0`).

Verification: `cargo test -p viola-plugin-abi` passes (21 tests).

## Lock criteria

All CHANGE blocks verify; `cargo test -p viola-plugin-abi` green;
`cargo mock check` green; no `mockspace.toml` severity edits.

## Comparison to deprecated changelists

None.
