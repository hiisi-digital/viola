# Topic: document the project-scope lint ABI in the design doc

**Date:** 2026-05-28
**Phase:** TOPIC
**Scope:** Update `viola-plugin-abi/DESIGN.md.tmpl` to describe the project-scope lint surface shipped in round 202605281600: the `viola.lint.evaluate-project.v1` provider, `LintEvaluateProjectIndexVtable`, and the `IndexBatch` carrier. No source changes.

**Source TOPIC:** round 202605281600 (project-scope ABI), on this branch.

# Why this round exists

Round 202605281600 added the project-scope lint ABI to `viola-plugin-abi`. The design doc's Provider IDs and Vtable shapes sections describe only the runner, grammar, and single-phase lint surfaces. The `docs=design` contract wants the doc to describe the shipped surface, so this round adds the project-scope provider, its two-phase vtable, and the `IndexBatch` carrier.

# What lands

- `viola-plugin-abi/DESIGN.md.tmpl`: a `PROVIDER_LINT_EVALUATE_PROJECT` entry under Provider IDs; a `LintEvaluateProjectIndexVtable` entry under Vtable shapes describing the two-phase index-then-evaluate dispatch; and an `IndexBatch` description covering its host-owned, opaque, overflow-reporting contract plus the `MAX_INDEX_ENTRIES` default cap.

# Lock criteria

DOC CL locks when the CHANGE block names its file and every backticked type it adds exists in source. No SRC CL change. `cargo mock check` green.

# Comparison to deprecated changelists

None.
