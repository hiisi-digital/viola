# Doc changelist: round 202605281700

**Phase:** DOC (living until lock)
**Topic:** `202605281700_topic.project-scope-abi-doc.md`
**Scope:** Document the project-scope lint ABI in `viola-plugin-abi/DESIGN.md.tmpl`. No source changes (shipped in round 202605281600).

## What ships

The Provider IDs and Vtable shapes sections gain the project-scope entries, and the `IndexBatch` carrier is described. All exist in source as of round 202605281600.

## CHANGE: `viola-plugin-abi/DESIGN.md.tmpl` gains the project-scope lint surface

File: `viola/mock/crates/viola-plugin-abi/DESIGN.md.tmpl`. Under `## Provider IDs`, add a `### PROVIDER_LINT_EVALUATE_PROJECT` subsection (`viola.lint.evaluate-project.v1`, points at `LintEvaluateProjectIndexVtable`, distinct id keeping vtable shapes append-only). Under `## Vtable shapes`, add a `### LintEvaluateProjectIndexVtable` subsection describing the two-phase `index_phase` / `evaluate_phase` dispatch and the `IndexBatch` carrier (host-owned, opaque `entries`, `capacity` / `len` / `needed`, `MAX_INDEX_ENTRIES` default cap, `AbiStatus::Internal` overflow reporting).

Verification: `grep "LintEvaluateProjectIndexVtable" viola/mock/crates/viola-plugin-abi/DESIGN.md.tmpl` returns at least one match; `grep "IndexBatch" viola/mock/crates/viola-plugin-abi/DESIGN.md.tmpl` returns at least one match; every backticked type added exists in source.

## Lock criteria

The CHANGE block landed; the design-doc-source-mismatch lint passes; `cargo mock check` green. No SRC CL change.

## Comparison to deprecated changelists

None.
