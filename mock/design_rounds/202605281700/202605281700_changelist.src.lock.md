# Source changelist: round 202605281700

**Phase:** SRC (living until lock)
**Topic:** `202605281700_topic.project-scope-abi-doc.md`
**Doc CL:** `202605281700_changelist.doc.lock.md`
**Scope:** No source changes. This round updates `viola-plugin-abi/DESIGN.md.tmpl` only; the project-scope ABI source shipped under round 202605281600.

## No source changes

The project-scope ABI surface (`PROVIDER_LINT_EVALUATE_PROJECT`, `LintEvaluateProjectIndexVtable`, `IndexBatch`, `MAX_INDEX_ENTRIES`) shipped under round 202605281600's SRC CL and is locked. This round touches only the design-doc template, handled under its DOC CL.

Verification: `git diff --name-only <round-base>..HEAD -- 'mock/crates/**/*.rs'` returns no entries for this round's commits.

## Lock criteria

DOC CL locked; no `.rs` source touched in this round; `cargo mock check` green.

## Comparison to deprecated changelists

None.
