**Date:** 2026-05-25
**Phase:** SRC
**Scope:** Source changes for Slice 5a. Flips `Nam` from the Slice 1 ZST placeholder to a fields-bearing carrier of `viola_plugin_abi::NamPayload`. The `RunRunner::execute` body stays `unimplemented!()` per the locked DOC CL; Slice 5b lands the body.
**Source DOC:** `mock/design_rounds/202605250300_changelist.doc.lock.md`.

# Source changes

## CHANGE: struct `Nam` FLIPPED from ZST to fields-bearing in viola-core wus module
File: `mock/crates/viola-core/src/wus/mod.rs`
Verification: `grep -q "pub payload: viola_plugin_abi::NamPayload" mock/crates/viola-core/src/wus/mod.rs`.

Slice 1 declared `pub struct Nam;`. Slice 5a flips to `pub struct Nam { pub payload: viola_plugin_abi::NamPayload }`. Derives `Copy, Clone` only; `Debug` intentionally omitted per the SHAME entry (the inner `NamPayload` does not derive `Debug` upstream). Satisfies `ColumnValue: Copy + 'static`.

# Verification target

- `cargo check -p viola-core` clean.
- The `Nam` shape change does not require an AccessSet update; the Slice 1 `Read = Cons<Column<FileInfo>, Empty>` and `Write = Cons<Column<Nam>, Empty>` declarations stay. The body stays `unimplemented!()` and Slice 5b lands the AccessSet refinement alongside the body.
