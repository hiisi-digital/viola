**Date:** 2026-05-25
**Phase:** DOC
**Scope:** Slice 5a doc CL. Lands the `Nam` ZST flip DESIGN section, seven BACKLOG entries that ratify the Slice 5b body deferrals (FileEntryBuffer Resource plus six design tensions), and a `viola-core/SHAME.md.tmpl` `## Nam` entry for the Debug-derive omission. The RunRunner body stays `unimplemented!()` until Slice 5b lands the supporting Resources.
**Source TOPIC:** `mock/design_rounds/202605250300_topic.viola-254-slice-5a-runrunner-design.md`.

# Doc changes

## CHANGE: doc section `### Nam` REVISED in viola-core DESIGN.md.tmpl
File: `mock/crates/viola-core/DESIGN.md.tmpl`
Verification: `grep -q "^### \`Nam\`" mock/crates/viola-core/DESIGN.md.tmpl && grep -q "payload: viola_plugin_abi::NamPayload" mock/crates/viola-core/DESIGN.md.tmpl`.

The Slice 1 ZST placeholder section flips to a fields-bearing one-field shape: `pub struct Nam { pub payload: viola_plugin_abi::NamPayload }`. Documents the host-side ownership contract for the inner pointer (plugin-owned memory immutable for the invocation duration; the host reads only), the singleton-row convention on `Column<Nam>` for the scope-shaped runner (one column row per scheduler run for now), the `Debug`-derive omission and the SHAME entry tracking it.

## CHANGE: BACKLOG entry `RunRunner body and FFI invocation` ADDED to viola-core/BACKLOG.md.tmpl
File: `mock/crates/viola-core/BACKLOG.md.tmpl`
Verification: `grep -q "RunRunner body and FFI invocation" mock/crates/viola-core/BACKLOG.md.tmpl`.

Slice 5a flips `Nam` but the `RunRunner::execute` body stays `unimplemented!()`. Slice 5b lands the body proper plus the supporting Resources named below.

## CHANGE: BACKLOG entry `FileEntryBuffer Resource for RunRunner` ADDED to viola-core/BACKLOG.md.tmpl
File: `mock/crates/viola-core/BACKLOG.md.tmpl`
Verification: `grep -q "FileEntryBuffer Resource for RunRunner" mock/crates/viola-core/BACKLOG.md.tmpl`.

The FFI `RunScope.files: *const FileEntry` needs a stable backing array. Slice 5b adds a host-shim-populated `Resource<FileEntryBuffer>` paralleling `DiscoveredFilePaths`, sized at `MAX_DISCOVERED_FILES`.

## CHANGE: BACKLOG entry `FileEntry enrichment for RunScope` ADDED to viola-core/BACKLOG.md.tmpl
File: `mock/crates/viola-core/BACKLOG.md.tmpl`
Verification: `grep -q "FileEntry enrichment for RunScope" mock/crates/viola-core/BACKLOG.md.tmpl`.

`FileEntry` carries `language: BytesRef`, `hash: BytesRef`, `size_bytes: u64` beyond `path`. The host shim today knows only paths. Slice 5b ships empties for the enrichment fields; subsequent refinement adds language detection, hash computation, and stat-based size population at host-shim time.

## CHANGE: BACKLOG entry `runner_vtable_from_library helper for viola-core invoke` ADDED to viola-core/BACKLOG.md.tmpl
File: `mock/crates/viola-core/BACKLOG.md.tmpl`
Verification: `grep -q "runner_vtable_from_library helper" mock/crates/viola-core/BACKLOG.md.tmpl`.

The existing `viola_core::invoke::runner_vtable` takes a pre-#254 `Extension`. Slice 5b adds a `runner_vtable_from_library(&Library) -> Maybe<&'static RunnerExecuteScopeVtable>` helper that walks the descriptor's provider table directly.

## CHANGE: BACKLOG entry `host_ctx model for FFI plugin callbacks` ADDED to viola-core/BACKLOG.md.tmpl
File: `mock/crates/viola-core/BACKLOG.md.tmpl`
Verification: `grep -q "host_ctx model for FFI plugin callbacks" mock/crates/viola-core/BACKLOG.md.tmpl`.

The FFI signature passes `host_ctx: *mut c_void`. Slice 5b passes null. Trigger for the real model: Slice 8 host-shim wire-up plus a follow-up that introduces `Resource<HostCallbackTable>` carrying the callback vtable per cdylib boundary memo Shape D. Until then null is the documented contract.

## CHANGE: BACKLOG entry `Resource<RunSurface> and CiState surface field` ADDED to viola-core/BACKLOG.md.tmpl
File: `mock/crates/viola-core/BACKLOG.md.tmpl`
Verification: `grep -q "Resource<RunSurface> and CiState surface field" mock/crates/viola-core/BACKLOG.md.tmpl`.

`RunScope.surface: RunSurface` needs a host-shim-picked value. `Resource<CiState>` carries `is_ci` but not `surface`. Slice 5b hardcodes `RunSurface::Cli` or introduces `Resource<RunSurface>`. The choice waits on viola-cli host-shim shape.

## CHANGE: BACKLOG entry `Workspace path Str-to-bytes resolution for FFI` ADDED to viola-core/BACKLOG.md.tmpl
File: `mock/crates/viola-core/BACKLOG.md.tmpl`
Verification: `grep -q "Workspace path Str-to-bytes resolution for FFI" mock/crates/viola-core/BACKLOG.md.tmpl`.

`RunScope.workspace_root: BytesRef` needs raw bytes. `Resource<Workspace>.path: Str` is an interned handle. Resolution depends on the host-shim long-lived interner (Slice 2b BACKLOG entry). Slice 5b ships `BytesRef::EMPTY` until that lands.

## CHANGE: SHAME entry `## Nam` ADDED to viola-core/SHAME.md.tmpl
File: `mock/crates/viola-core/SHAME.md.tmpl`
Verification: `grep -q "^## Nam$" mock/crates/viola-core/SHAME.md.tmpl`.

`Nam` ships without the Debug derive the locked DOC CL committed (Copy, Clone, Debug would be the standard column-record shape). The omission is upstream-forced: `viola_plugin_abi::NamPayload` carries `data: *const c_void` and does not derive Debug in current `viola-plugin-abi`. Mirrors the Slice 3b PluginEntry precedent: track the discipline gap per `cl-claim-sketch-discipline.md` so the design-doc-source-mismatch lint stays quiet on the documented type.

# Verification target

After the round ships: `cargo check` clean. `cargo mock --lint-only --commit` clean. `Nam` satisfies `Copy + 'static + ColumnValue`. The SHAME `## Nam` entry exists with a 50+ word explanation.
