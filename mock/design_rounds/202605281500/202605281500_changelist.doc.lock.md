# Doc changelist: round 202605281500

**Phase:** DOC (living until lock) **Topic:**
`202605281500_topic.nam-v1-1-0-review-fixes.md` **Scope:** Post-review hardening
of `viola-plugin-abi/src/nam.rs`: refresh the stale module-head doc and add
whole-multiple guards to the slice accessors.

## What ships

Two pre-merge review findings on the NAM v1.1.0 change: a stale module-head
schema section and missing record-size guards in `nam_file_entries` /
`nam_file_nodes`.

## CHANGE: `nam.rs` module-head schema doc refreshed for v1.x

File: `viola/mock/crates/viola-plugin-abi/src/nam.rs`. Rewrite the
`# NAM v1.0.0 wire schema` module-doc section to describe the v1.x line: the
`NamFileEntry` slice (v1.0.0) plus the per-file `NamNode` array (v1.1.0), walked
via `nam_file_entries` / `nam_file_nodes`, with the accessor gated on major
version `1` rather than exact `V1_0_0`.

Verification:
`! grep "is not v1.0.0" viola/mock/crates/viola-plugin-abi/src/nam.rs`;
`grep "v1.x" viola/mock/crates/viola-plugin-abi/src/nam.rs` returns at least one
match.

## CHANGE: whole-multiple guard added to `nam_file_entries` and `nam_file_nodes`

File: `viola/mock/crates/viola-plugin-abi/src/nam.rs`. In each accessor add a
`debug_assert!` that the carrier byte length is an exact multiple of the record
size before constructing the slice, and extend the SAFETY note to state the
contiguous-and-aligned precondition.

Verification:
`grep "debug_assert" viola/mock/crates/viola-plugin-abi/src/nam.rs` returns at
least two matches.

## Lock criteria

Both CHANGE blocks landed; `cargo test -p viola-plugin-abi` passes;
`cargo mock check` green.

## Comparison to deprecated changelists

None.
