# Source changelist: round 202605281500

**Phase:** SRC (living until lock)
**Topic:** `202605281500_topic.nam-v1-1-0-review-fixes.md`
**Doc CL:** `202605281500_changelist.doc.lock.md`
**Scope:** Apply the two post-review hardening edits in `viola-plugin-abi/src/nam.rs`.

## CHANGE: `nam.rs` module-head schema doc refreshed for v1.x

File: `mock/crates/viola-plugin-abi/src/nam.rs`. Replace the `# NAM v1.0.0 wire schema` module-doc section with a v1.x description naming the entry slice, the v1.1.0 node array, both accessors, and the major-version gate.

Verification: `! grep "is not v1.0.0" mock/crates/viola-plugin-abi/src/nam.rs`; `grep "v1.x" mock/crates/viola-plugin-abi/src/nam.rs` returns at least one match.

## CHANGE: whole-multiple guard in `nam_file_entries` and `nam_file_nodes`

File: `mock/crates/viola-plugin-abi/src/nam.rs`. Add `debug_assert!(len % record_size == 0, ...)` before each `from_raw_parts`, and extend each SAFETY comment to name the contiguous-and-aligned precondition.

Verification: `grep "debug_assert" mock/crates/viola-plugin-abi/src/nam.rs` returns at least two matches.

## Lock criteria

Both CHANGE blocks applied; `cargo test -p viola-plugin-abi` passes; `cargo mock check` green.

## Comparison to deprecated changelists

None.
