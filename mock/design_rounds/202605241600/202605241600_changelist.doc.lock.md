**Date:** 2026-05-24
**Phase:** DOC
**Scope:** No `*.md.tmpl` doc changes in this round. The Slice 1 scaffolding lands stubs whose `### TypeName` sections in `viola-core/DESIGN.md.tmpl` will be authored by the implementing slices (2 through 7) that flesh each WU body. This round only opens the source surface; the doc surface is updated where the bodies land.

# Doc changes

None. `viola-core/DESIGN.md.tmpl` already references the WorkUnit type names (`LoadConfig`, `LoadPlugins`, `DiscoverFiles`, `RunRunner`, `RunLint<const L: usize>`, `EmitDiagnostics`) in the "Locked target state" prose. The Slice 1 source introduces these as stub types with `unimplemented!()` bodies and the AccessSet declarations the scheduler reads at plan time, plus three Resource value types (`Workspace`, `CiState`, `RunSurface`). Each gains its own `### TypeName` section in DESIGN.md.tmpl during the slice that fleshes its body.

The interim scaffolding types (`Session`, `runner_vtable`, `grammar_vtable`, `lint_vtable`, `run`, `PipelineReport`, `FailureRecord`, `cmp_diag`, `sort_diagnostics`, `Role`, `RoleSet`, `roles_of`, `is_runner`, `is_grammar`, `is_lint`, `LintConfig`) survive unchanged for this round; their dissolution lands in Slice 8 along with the `viola-cli` rewire to `scheduler.run()`.

# Out of scope

Subsequent slice rounds open their own design rounds with per-slice doc CLs. Slice 2 (`LoadConfig`), Slice 3 (`LoadPlugins`), and onward each carry the doc edits for the type they implement.
