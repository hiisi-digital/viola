**Date:** 2026-05-25 **Phase:** SRC **Scope:** Slice 8a source. Deletes pre-#254
viola-core surfaces (`pipeline.rs`, `session.rs`, `tests/pipeline_e2e.rs`).
Trims viola-core `lib.rs` re-exports. Stubs viola-cli `main.rs` run paths with
`unimplemented!("Slice 8b ships viola-cli scheduler rewire")` and drops the
local CaptureSink + impls + helper functions tied to the deleted surface.
**Source TOPIC:**
`mock/design_rounds/202605250900_topic.viola-254-slice-8a-viola-core-cleanup.md`.
**Source DOC CL:** `mock/design_rounds/202605250900_changelist.doc.lock.md`.

# Source changes

## CHANGE: module `viola_core::pipeline` REMOVED from viola-core

File: `mock/crates/viola-core/src/pipeline.rs` Verification:
`! test -f mock/crates/viola-core/src/pipeline.rs`.

The pre-#254 in-process orchestrator function `viola_core::pipeline::run` and
its supporting types go away entirely. Replaced by the seven WUs composed at
scheduler-build time (Slice 8b).

## CHANGE: module `viola_core::session` REMOVED from viola-core

File: `mock/crates/viola-core/src/session.rs` Verification:
`! test -f mock/crates/viola-core/src/session.rs`.

The `Session<const N: usize>` fixed-cap LIFO container goes away. Scheduler
lifetime owns reverse-shutdown ordering post-#254.

## CHANGE: integration test `viola-core/tests/pipeline_e2e.rs` REMOVED

File: `mock/crates/viola-core/tests/pipeline_e2e.rs` Verification:
`! test -f mock/crates/viola-core/tests/pipeline_e2e.rs`.

The pipeline_e2e integration test exercised `pipeline::run` against runner +
lint fixture cdylibs. The function it tested is gone; the test is gone with it.
A new scheduler-driven e2e test waits on hilavitkutin's `Scheduler::run` body
landing.

## CHANGE: `viola_core::lib` REVISED to drop pipeline + session module decls + Session re-export

File: `mock/crates/viola-core/src/lib.rs` Verification:
`! grep -q "^pub mod pipeline" mock/crates/viola-core/src/lib.rs && ! grep -q "^pub mod session" mock/crates/viola-core/src/lib.rs && ! grep -q "^pub use session" mock/crates/viola-core/src/lib.rs`.

`pub mod pipeline;` removed; `pub mod session;` removed;
`pub use session::Session;` removed. The rustdoc bullet documenting `pipeline`
and `session` is dropped. The `Extension`, `ExtensionHost`, `ExtensionMeta` etc
re-exports from `hilavitkutin_extensions` stay (FFI surface; viola-cli still
needs them in its stubbed state and Slice 8b settles the ExtensionHost-name
disambiguation).

## CHANGE: `viola_cli::main` run paths STUBBED to `unimplemented!` and pre-#254 surface dropped

File: `mock/crates/viola-cli/src/main.rs` Verification:
`grep -q "unimplemented!.*Slice 8b" mock/crates/viola-cli/src/main.rs && ! grep -Eq "^\s*use viola_core::.*Session" mock/crates/viola-cli/src/main.rs && ! grep -Eq "^\s*use viola_core::.*pipeline" mock/crates/viola-cli/src/main.rs`
(doc-comment mentions of the deleted surfaces do not trip the predicate; the
negation is against live `use` declarations).

The `run_v1` and `run_v2` function bodies become a single
`unimplemented!("Slice 8b ships viola-cli scheduler rewire")` line each at the
top of the function (the function declarations stay for libc-entry-point
continuity; the arg-parsing and config-reading paths feeding into them stay;
only the actual lint-running logic stubs out). The pre-#254 imports drop:
`pipeline::run`, `Session`, `RunScope` (the field-bearing usage), `RunSurface`
usage (if scoped to deleted paths), the local `Diagnostic`-flavoured
aggregation. The local `CaptureSink` struct, its `Push<Diagnostic>` and `Len`
impls, and helper functions wired to the deleted surface (decimal formatting
tied to Diagnostic emission, the BytesRef arena management for CaptureSink) drop
in concert. The libc entry point, panic handler, `rust_eh_personality`, arg
parsing, config reading, and exit-code constants remain intact so the binary
continues to compile and run with documented panic behaviour on the lint paths.

# Verification target

- `cargo check -p viola-core` clean.
- `cargo check -p viola-cli` clean.
- `cargo build -p viola-cli` succeeds.
- `cargo test -p viola-core` passes the six AccessSet witness tests
  (pipeline_e2e gone).
- `cargo mock --lint-only --commit` clean.
- All five `## CHANGE:` verification predicates pass.
