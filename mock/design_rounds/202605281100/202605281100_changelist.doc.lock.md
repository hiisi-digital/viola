# Doc changelist: round 202605281100

**Phase:** DOC (living until lock)
**Topic:** `202605281100_topic.abi-v2-design-doc-reconcile.md`
**Scope:** Reconcile the four per-crate `DESIGN.md.tmpl` design docs with the ABI v2 host-owned-buffer shape shipped in round 202605281000. No source changes.

## What ships

Round 202605281000 deleted `DiagnosticBatch`, changed `LintEvaluateVtable.evaluate` to take `out_entries` / `out_capacity` / `out_len`, and moved the provider id to `viola.lint.evaluate.v2`. The per-crate design docs still describe the v1 `DiagnosticBatch` shape. The `docs=design` contract requires the living design docs to describe shipped source, so this round rewrites the three affected docs. Every backticked type name removed (`DiagnosticBatch`) or kept (`Diagnostic`) is checked against current source.

## CHANGE: `viola-plugin-abi/DESIGN.md.tmpl` lint provider + vtable + carrier rewritten to v2

File: `viola/mock/crates/viola-plugin-abi/DESIGN.md.tmpl`. The `PROVIDER_LINT_EVALUATE` description moves from `viola.lint.evaluate.v1` producing a `DiagnosticBatch` to `viola.lint.evaluate.v2` writing into a host-owned `Diagnostic` buffer. The `LintEvaluateVtable` signature line is rewritten to the seven-argument host-owned-buffer shape with the overflow contract. The `### DiagnosticBatch` subsection under `## Diagnostic wire shape` is deleted; the section lead-in is reworded for the host-owned buffer. The provider-count lead-in ("Three v1 provider IDs") notes the lint role is at v2.

Verification: `grep "viola.lint.evaluate.v2" viola/mock/crates/viola-plugin-abi/DESIGN.md.tmpl` returns at least one match; `! grep "DiagnosticBatch" viola/mock/crates/viola-plugin-abi/DESIGN.md.tmpl`.

## CHANGE: `viola-core/DESIGN.md.tmpl` RunLint host-call rewritten to host-owned buffer

File: `viola/mock/crates/viola-core/DESIGN.md.tmpl`. The Slice 6b body description (the `DiagnosticBatch { entries: null, len: 0 }` build and the "returned `DiagnosticBatch`" projection) is rewritten: the body fills a host-owned `MaybeUninit<[Diagnostic; MAX_DIAGS_PER_LINT]>` buffer, passes `out_entries` / `out_capacity` (= `MAX_DIAGS_PER_LINT`) / `out_len`, and projects the written prefix on both `AbiStatus::Ok` and `AbiStatus::Internal` (overflow truncation).

Verification: `grep "out_len\|out_capacity" viola/mock/crates/viola-core/DESIGN.md.tmpl` returns at least one match; `! grep "DiagnosticBatch" viola/mock/crates/viola-core/DESIGN.md.tmpl`.

## CHANGE: `viola-deno-runtime/DESIGN.md.tmpl` LintEvalProvider + Arena note rewritten to v2

File: `viola/mock/crates/viola-deno-runtime/DESIGN.md.tmpl`. The `LintEvalProvider` description moves from `viola.lint.evaluate.v1` receiving a `DiagnosticBatch` to `viola.lint.evaluate.v2` writing into the host-owned `Diagnostic` buffer. The `Arena` v1-contract sentence is reworded for the v2 write-through shape.

Verification: `grep "viola.lint.evaluate.v2" viola/mock/crates/viola-deno-runtime/DESIGN.md.tmpl` returns at least one match; `! grep "DiagnosticBatch" viola/mock/crates/viola-deno-runtime/DESIGN.md.tmpl`.

## CHANGE: `viola-test-plugin-fixture/DESIGN.md.tmpl` LintEvalProvider rewritten to v2

File: `viola/mock/crates/viola-test-plugin-fixture/DESIGN.md.tmpl`. The `LintEvalProvider` description moves from the `viola.lint.evaluate.v1` contract emitting a diagnostic batch to the `viola.lint.evaluate.v2` contract writing into the host-owned `Diagnostic` buffer.

Verification: `grep "viola.lint.evaluate.v2" viola/mock/crates/viola-test-plugin-fixture/DESIGN.md.tmpl` returns at least one match; `! grep "lint.evaluate.v1" viola/mock/crates/viola-test-plugin-fixture/DESIGN.md.tmpl`.

## Lock criteria

All four CHANGE blocks landed; verification commands return expected match counts; `! grep "DiagnosticBatch"` passes across the DESIGN docs; no `lint.evaluate.v1` remains in any DESIGN doc; `cargo mock check` green. No SRC CL change (source shipped in round 202605281000).

## Comparison to deprecated changelists

None.
