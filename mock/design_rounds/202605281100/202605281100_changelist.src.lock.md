# Source changelist: round 202605281100

**Phase:** SRC (living until lock) **Topic:**
`202605281100_topic.abi-v2-design-doc-reconcile.md` **Doc CL:**
`202605281100_changelist.doc.lock.md` **Scope:** No source changes. This round
reconciles the per-crate `DESIGN.md.tmpl` design docs with the ABI v2
host-owned-buffer source that round 202605281000 already shipped and locked.

## No source changes

The source for the ABI v2 host-owned buffer (the `LintEvaluateVtable.evaluate`
signature, the `DiagnosticBatch` deletion, the `viola.lint.evaluate.v2` provider
id, and the `RunLint` / fixture / deno-runtime trampolines) shipped under round
202605281000's SRC CL and is locked. This round touches only the four
`DESIGN.md.tmpl` files, which are doc-template surfaces handled entirely under
this round's DOC CL.

Verification: `git diff --name-only <round-base>..HEAD -- 'mock/crates/**/*.rs'`
returns no entries for this round's commits.

## Lock criteria

DOC CL locked; no `.rs` source touched in this round; `cargo mock check` green.

## Comparison to deprecated changelists

None.
