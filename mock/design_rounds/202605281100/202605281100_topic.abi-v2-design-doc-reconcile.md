# Topic: reconcile DESIGN docs with ABI v2 host-owned buffer

**Date:** 2026-05-28
**Phase:** TOPIC
**Scope:** Update the living `DESIGN.md.tmpl` design docs to describe the ABI v2 lint-evaluate shape that round 202605281000 already shipped into source. Touches `viola-plugin-abi/DESIGN.md.tmpl`, `viola-core/DESIGN.md.tmpl`, `viola-deno-runtime/DESIGN.md.tmpl`. No source changes.

**Source TOPIC:** round 202605281000 (ABI v2 host-owned buffer), already locked and merged into this branch.

# Why this round exists

Round 202605281000 changed the lint-evaluate vtable to a host-owned output buffer: `DiagnosticBatch` was deleted, `evaluate` gained `out_entries` / `out_capacity` / `out_len`, and the provider id moved to `viola.lint.evaluate.v2`. That round's DOC CL was a design-plan narrative for the source change; it did not edit the per-crate `DESIGN.md.tmpl` files, which still describe the v1 `DiagnosticBatch` shape. The `docs=design` contract requires the living design docs to describe the shipped source, so this round reconciles them.

# What lands

- `viola-plugin-abi/DESIGN.md.tmpl`: the `PROVIDER_LINT_EVALUATE` provider-id description moves to `viola.lint.evaluate.v2`; the `LintEvaluateVtable` signature is rewritten to the seven-argument host-owned-buffer shape with the overflow contract; the `### DiagnosticBatch` subsection is deleted; the provider-count and diagnostic-wire-shape lead-ins are updated.
- `viola-core/DESIGN.md.tmpl`: the `RunLint` host-call description is rewritten to the host-owned `MaybeUninit<[Diagnostic; MAX_DIAGS_PER_LINT]>` buffer, `out_capacity` = `MAX_DIAGS_PER_LINT`, with `Ok` and `Internal` both treated as a valid written prefix.
- `viola-deno-runtime/DESIGN.md.tmpl`: the `LintEvalProvider` description moves to `viola.lint.evaluate.v2` and the host-owned buffer; the `Arena` v1-contract note is reworded for the v2 shape.

# Lock criteria

DOC CL locks when every CHANGE block names its file with a verification command and `! grep "DiagnosticBatch"` passes across the three DESIGN docs. There is no SRC CL change; the source already shipped in round 202605281000. `cargo mock check` green.

# Comparison to deprecated changelists

None.
