**Date:** 2026-05-25
**Phase:** DOC
**Scope:** Slice 8b.1 doc CL. Adds DESIGN.md.tmpl `## Slice 8b.1 patch fix + EmitWriter + ExtensionHost drop` section. Revises the Slice 8a `ExtensionHost re-export disambiguation` BACKLOG entry to mark "Shipped in Slice 8b.1". Updates the `viola-cli scheduler wiring and concrete EmitWriter impl` BACKLOG entry to record partial progress (StderrEmitter ships now; full scheduler chain ships in Slice 8b.2).
**Source TOPIC:** `mock/design_rounds/202605251100_topic.viola-254-slice-8b1-patch-emit-rexport.md`.

# Doc changes

## CHANGE: doc section `## Slice 8b.1 patch fix + EmitWriter + ExtensionHost drop` ADDED to viola-core DESIGN.md.tmpl
File: `mock/crates/viola-core/DESIGN.md.tmpl`
Verification: `grep -q "^## Slice 8b.1 patch fix" mock/crates/viola-core/DESIGN.md.tmpl`.

New section documenting the three Slice 8b.1 changes: (1) extending viola-mock-workspace's `[patch."ssh://git@github.com/orgrinrt/arvo.git"]` to cover all 16 arvo crates (was 6; missing 10); (2) dropping `viola_core::ExtensionHost` re-export from `hilavitkutin_extensions`; (3) shipping `viola_cli::emit::StderrEmitter` concrete `EmitWriter` impl. Names the patch-matrix root cause discovered during the failed full-Slice-8b attempt (cargo's dual-source resolution between path-patched `arvo` and git-fetched `arvo-sparse` produced misleading `Csr::row_col_indices` "method not found" errors). Names the path forward for Slice 8b.2: with the patch gap closed, adding `hilavitkutin` as a viola-cli dep unblocks the scheduler-build chain.

## CHANGE: BACKLOG entry `ExtensionHost re-export disambiguation` REVISED to mark shipped
File: `mock/crates/viola-core/BACKLOG.md.tmpl`
Verification: `grep -q "ExtensionHost re-export disambiguation" mock/crates/viola-core/BACKLOG.md.tmpl && grep -q "Shipped in Slice 8b.1" mock/crates/viola-core/BACKLOG.md.tmpl`.

Revised to record "Shipped in Slice 8b.1: dropped re-export". Consumers split: substrate type from `hilavitkutin_extensions::ExtensionHost`; Resource version from `viola_core::resources::ExtensionHost`. Sibling re-exports stay.

## CHANGE: BACKLOG entry `viola-cli scheduler wiring and concrete EmitWriter impl` UPDATED to record partial progress
File: `mock/crates/viola-core/BACKLOG.md.tmpl`
Verification: `grep -q "viola-cli scheduler wiring" mock/crates/viola-core/BACKLOG.md.tmpl && grep -q "Slice 8b.1 ships StderrEmitter" mock/crates/viola-core/BACKLOG.md.tmpl`.

Updated to note "Slice 8b.1 ships StderrEmitter concrete impl + the viola-mock-workspace patch fix that unblocks it". The full scheduler-build chain remains a Slice 8b.2 BACKLOG entry; this BACKLOG line stays open until Slice 8b.2 ships the chain.

# Verification target

After the round ships: `cargo check -p viola-core` + `cargo check -p viola-cli` + `cargo build -p viola-cli` succeed. `cargo test -p viola-core` passes. `cargo mock --lint-only --commit` clean. Three `## CHANGE:` predicates pass.
