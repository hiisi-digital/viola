**Date:** 2026-05-24
**Phase:** DOC
**Scope:** Docs-only round closing the Slice 2b audit-trail gap. Lands the missing `### TypeName` sections in `viola-core/DESIGN.md.tmpl` and `viola-config/DESIGN.md.tmpl` for the Slice 2b type additions, plus BACKLOG entries for the three deferred items the locked SRC CL named.
**Source TOPIC:** `mock/design_rounds/202605242350_topic.viola-254-slice-2b-design-sections.md`.

# Doc changes

## `mock/crates/viola-core/DESIGN.md.tmpl`

Two new `### TypeName` sections under a Slice 2b column-record family group, placed after the existing Slice 2a resource-shapes section.

## CHANGE: doc section `### WuDiagnostic` ADDED
File: `mock/crates/viola-core/DESIGN.md.tmpl`
Verification: `grep -q "^### \`WuDiagnostic\`" mock/crates/viola-core/DESIGN.md.tmpl`.

Documents the host-side per-finding record carried by `Column<WuDiagnostic>`. Names the four fields (`severity`, `source`, `message`, `range`), the derives, and the FFI distinction from `viola_plugin_abi::Diagnostic` (`#[repr(C)]` plugin-owned carrier with `BytesRef` strings). Notes the `EmitDiagnostics` (Slice 7) host-to-ABI projection at egress.

## CHANGE: doc section `### WuDiagnosticSource` ADDED
File: `mock/crates/viola-core/DESIGN.md.tmpl`
Verification: `grep -q "^### \`WuDiagnosticSource\`" mock/crates/viola-core/DESIGN.md.tmpl`.

Closed-vocabulary enum with six variants (`ConfigParse`, `PluginLoad`, `FileWalk`, `RunRunner`, `RunLint`, `Emit`). One variant per producer WU. Per `vocabulary.md`'s closed-enum-as-spec pattern (`Phase` precedent). No `Default` impl by design; construction sites pick the variant explicitly.

## `mock/crates/viola-config/DESIGN.md.tmpl`

Two new `### TypeName` sections under the Slice 2a additions block, plus a method-shape paragraph extending the existing `### ViolaConfigOwned` section.

## CHANGE: doc section `### ConfigArena` REVISED
File: `mock/crates/viola-config/DESIGN.md.tmpl`
Verification: `grep -q "cursor-plus-offsets-table" mock/crates/viola-config/DESIGN.md.tmpl`.

Slice 2a added the `### ConfigArena` heading naming the type as crate-private and forward-declared. This round revises the section to describe the real Slice 2b implementation: cursor-plus-offsets-table layout, `Cell<USize>` cursor, `UnsafeCell<[u8; N]>` byte buffer, `UnsafeCell<[(USize, USize); ARENA_MAX_ENTRIES]>` offsets table, `Cell<USize>` entries length. Names the 1-based external id scheme (id 0 reserved as overflow / never-interned sentinel). Names the `unsafe impl<const N: usize> Sync for ConfigArena<N>` contract pinning the AccessSet single-writer guarantee. Notes the aliasing contract (append-only across intern calls; resolved `&str` stays valid).

## CHANGE: doc section `### ARENA_MAX_ENTRIES` ADDED
File: `mock/crates/viola-config/DESIGN.md.tmpl`
Verification: `grep -q "^### \`ARENA_MAX_ENTRIES\`" mock/crates/viola-config/DESIGN.md.tmpl`.

Maximum number of distinct interned entries one `ConfigArena<N>` can carry. Workspace-default 256. Sizes the offsets table at the type level. Sibling MAX_PLUGINS pattern; ConstParamTy requires bare usize.

## CHANGE: method-shape paragraph for `populate_from_borrowed` ADDED to `### ViolaConfigOwned`
File: `mock/crates/viola-config/DESIGN.md.tmpl`
Verification: `grep -q "populate_from_borrowed" mock/crates/viola-config/DESIGN.md.tmpl`.

Documents the `populate_from_borrowed(&self, _borrowed: &ViolaConfig<'_, MAX_PLUGINS_CAP>)` method shape. Names the interior-mutability contract: `&self` because the bundled `ConfigArena` carries `Cell` plus `UnsafeCell`; the scheduler's AccessSet single-writer contract makes the receiver sound. States that Slice 2b ships a no-op body; producer slices flip owned-records and fill the projection.

## `mock/crates/viola-core/BACKLOG.md.tmpl`

## CHANGE: BACKLOG entry `runtime test-Ctx fixture for LoadConfig` ADDED
File: `mock/crates/viola-core/BACKLOG.md.tmpl`
Verification: `grep -q "test-Ctx fixture" mock/crates/viola-core/BACKLOG.md.tmpl`.

Names the deferred runtime parse-success / parse-failure tests. The Slice 1 WuCtxStub::Stub `unimplemented!()`s every API method; runtime tests need a real test-Ctx fixture backing every Resource and Column with concrete storage. UNBACKTICKED naming so the design-doc-source-mismatch lint stays quiet.

## CHANGE: BACKLOG entry `host-shim long-lived interner wiring` ADDED
File: `mock/crates/viola-core/BACKLOG.md.tmpl`
Verification: `grep -q "host-shim long-lived interner" mock/crates/viola-core/BACKLOG.md.tmpl`.

The public Str surface ships only `__make` / `__runtime` constructors keyed by `Bits<28, Hot>` ids. The parse-failure message in LoadConfig uses `Str::default()` until the host-shim interner is registered at scheduler-builder time. Follow-up wires the interner.

## `mock/crates/viola-config/BACKLOG.md.tmpl` (NEW)

## CHANGE: file `viola-config/BACKLOG.md.tmpl` NEW
File: `mock/crates/viola-config/BACKLOG.md.tmpl`
Verification: `test -f mock/crates/viola-config/BACKLOG.md.tmpl`.

New file establishing the BACKLOG surface for viola-config. Inherits the format used by viola-core's BACKLOG.

## CHANGE: BACKLOG entry `populate_from_borrowed owned-record fill-in` ADDED
File: `mock/crates/viola-config/BACKLOG.md.tmpl`
Verification: `grep -q "populate_from_borrowed" mock/crates/viola-config/BACKLOG.md.tmpl`.

The method body is a no-op for Slice 2b. The owned-record fields (`PluginEntryOwned`, `GateOwned`, `RuleOwned`, `PartialRuleOwned`) are zero-sized placeholders; nothing to project until each producer slice flips its owned-record to fields-bearing. Slice 3 (LoadPlugins) revisits for plugins; validator slices revisit for gates / rules / partial rules.

# Verification target

After the round ships:

- `cargo check` clean across viola-config and viola-core (no source change should regress).
- `cargo mock --lint-only --commit` clean.
- Every backticked Slice 2b type name in viola-core / viola-config DESIGN.md.tmpl exists in source.
- BACKLOG entries are UNBACKTICKED.
