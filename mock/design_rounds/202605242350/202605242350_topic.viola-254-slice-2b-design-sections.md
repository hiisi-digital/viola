**Date:** 2026-05-24
**Phase:** TOPIC
**Scope:** A docs-only round that closes the audit-trail gap left by Slice 2b. Lands the missing `### TypeName` sections in `viola-core/DESIGN.md.tmpl` and `viola-config/DESIGN.md.tmpl` for the Slice 2b type additions (`WuDiagnostic`, `WuDiagnosticSource`, `ConfigArena<N>`, `ARENA_MAX_ENTRIES`, `populate_from_borrowed`), plus BACKLOG entries for the three deferred items the locked SRC CL named.
**Source topics:** `mock/design_rounds/202605242200/` (closed Slice 2b round). The locked SRC CL there shipped real source for every code CHANGE block; this docs round closes the doc-coverage side.

# Why this round exists

Round 202605242200 (Slice 2b) merged across three PRs: #31 (types + AccessSet), #32 (LoadConfig body + AccessSet witness test), #33 (round lock + close). The source landed but `WuDiagnostic`, `WuDiagnosticSource`, and the real `ConfigArena<N>` shape have no `### TypeName` sections in their owning crate's DESIGN.md.tmpl. The `design-doc-source-mismatch` lint exits clean for now because the types are referenced only through their backticked names already covered by Slice 2a sections, but the audit-trail expectation is that every shipped type carries a DESIGN paragraph naming the shape and the why.

Slice 2a established the precedent: when a body slice lands types, a follow-up docs round adds the DESIGN sections. Round 202605242100 was that round for Slice 2a. This round is the analogous follow-up for Slice 2b.

The locked SRC CL at `mock/design_rounds/202605242200/202605242200_changelist.src.lock.md` names three deferred items:

- Runtime test-Ctx fixture backing every `Resource<...>` and `Column<...>` with concrete storage. The Slice 1 `WuCtxStub::Stub` calls `unimplemented!()` on every API method; a real test-Ctx is needed to exercise the body. Deferred to a follow-up PR.
- Host-shim long-lived interner wiring. The public `Str` surface ships only `__make` / `__runtime` constructors gated by `Bits<28, Hot>` ids; parse-failure messages use `Str::default()` until the interner is registered at scheduler-builder time.
- Owned-record fill-in inside `populate_from_borrowed`. The `PluginEntryOwned`, `GateOwned`, `RuleOwned`, `PartialRuleOwned` records are ZST placeholders; nothing to project until each producer slice flips its record type. The method body stays a no-op for Slice 2b.

PR #33 reviewer specifically flagged that these deferred items should be tracked as concrete BACKLOG entries before Slice 3 opens, so the locked-claim audit trail does not silently accrue debt. This round folds the BACKLOG entries in alongside the DESIGN sections.

# What lands

Two new `### TypeName` sections in `viola-core/DESIGN.md.tmpl` under a Slice 2b column-record family:

- `### WuDiagnostic`
- `### WuDiagnosticSource`

Two new sections in `viola-config/DESIGN.md.tmpl`:

- `### ConfigArena` (replaces a hypothetical Slice 2a stub mention; the real cursor-plus-offsets-table impl)
- `### ARENA_MAX_ENTRIES`

One new method-shape paragraph extending the existing `### ViolaConfigOwned` section:

- `populate_from_borrowed(&self, _borrowed)` method shape, interior-mutability contract, no-op body until owned-records flip.

Three new BACKLOG entries:

- `viola-core/BACKLOG.md.tmpl`: runtime test-Ctx fixture, host-shim long-lived interner wiring.
- `viola-config/BACKLOG.md.tmpl` (NEW file): populate_from_borrowed owned-record fill-in.

# Out of scope

- Slice 3 (LoadPlugins body). Opens its own round after this docs round closes.
- Touch-ups to viola-core / viola-config source. This is a docs-only round.
- Mockspace docs-only-round-shape question raised by PR #27 reviewer (trivial SRC CL required by mockspace's round-close workflow). Still a workspace-rule follow-up. This round follows the same two-commit shape as PR #27.

# Verification target

After the round ships:

- `cargo check` clean across viola-config and viola-core (no source change should regress).
- `cargo mock --lint-only --commit` clean.
- Every backticked Slice 2b type name in viola-core / viola-config DESIGN.md.tmpl exists in source.
- BACKLOG entries are UNBACKTICKED per `mock-workspace.md` rule.
