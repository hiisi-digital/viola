**Date:** 2026-05-25
**Phase:** SRC
**Scope:** viola-cli Scheduler::run Outcome handling source CL. One source edit on viola-cli/src/main.rs replacing the `let _ = scheduler.run(); EXIT_OK` discard with a `match` block.
**Source TOPIC:** `mock/design_rounds/202605250659_topic.viola-cli-scheduler-outcome-handling.md`.
**Source DOC:** `mock/design_rounds/202605250659_changelist.doc.lock.md`.

# Source changes

## CHANGE: `scheduler.run()` call FROM discard TO Outcome match in viola-cli/src/main.rs
File: `mock/crates/viola-cli/src/main.rs`
Verification: `! grep -q 'let _ = scheduler.run()' mock/crates/viola-cli/src/main.rs && grep -q 'notko::Outcome::Ok' mock/crates/viola-cli/src/main.rs && grep -q 'notko::Outcome::Err' mock/crates/viola-cli/src/main.rs`.

Replaces the `let _ = scheduler.run(); EXIT_OK` pattern with `match scheduler.run() { notko::Outcome::Ok(_) => EXIT_OK, notko::Outcome::Err(_) => EXIT_PLUGIN }`. The wave-y deferral-mentioning comment block above the call updates to describe the now-shipped wiring + name the engine-state prerequisite (hilavitkutin PR #83 returning `Cfg::Out::default() = Outcome::Ok(())`). The `Err` arm is unreachable in practice today (engine no-op always returns Ok) but exists for forward compatibility per the BACKLOG follow-up.

# Lock criteria

- The `## CHANGE:` verification predicate passes.
- `cargo build -p viola-cli` clean.
- `cargo test --workspace` clean.
