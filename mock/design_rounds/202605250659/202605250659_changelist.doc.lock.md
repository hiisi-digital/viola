**Date:** 2026-05-25 **Phase:** DOC **Scope:** viola-cli Scheduler::run Outcome
handling DOC CL. Revises the existing BACKLOG entry to mark shipped. **Source
TOPIC:**
`mock/design_rounds/202605250659_topic.viola-cli-scheduler-outcome-handling.md`.

# Doc changes

## CHANGE: BACKLOG entry `viola-cli Scheduler::run Outcome handling` REVISED to "Shipped"

File: `mock/crates/viola-core/BACKLOG.md.tmpl` Verification:
`grep -q "Shipped in viola-cli Scheduler::run Outcome handling" mock/crates/viola-core/BACKLOG.md.tmpl`.

Revise the entry body from "Slice 8b.2's scheduler-build chain ends with
`let _ = scheduler.run()`, intentionally discarding..." to "Shipped: viola-cli's
unix-main matches `Outcome::Ok(_) -> EXIT_OK`, `Outcome::Err(_) -> EXIT_PLUGIN`.
Hilavitkutin's engine body (PR #83) returns `Outcome::Ok(())` cleanly as the
no-op transitional shape; the Err arm is unreachable until the real morsel loop
lands but stays in place for forward compatibility per pre-1.0 churn
discipline." Per the no-legacy-shims-pre-1.0 convention this is an in-place
revision, not removal.

# Verification target

After the round ships: `cargo build -p viola-cli` clean.
`cargo test --workspace` clean. viola-cli's lint path returns the matched exit
code instead of unconditionally EXIT_OK.
