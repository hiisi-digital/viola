**Date:** 2026-05-25
**Phase:** DOC
**Scope:** Slice 5b doc CL. Adds `### FileEntryBuffer` DESIGN section and `### runner_vtable_from_library` DESIGN section. Revises the `### Nam` section's body-shape paragraph to record the Slice 5b body landing. Revises the existing `### RunRunner Slice 2a revision` section to a `### RunRunner Slice 5b body` revision documenting the AccessSet refinement and the body. Marks three Slice 5a BACKLOG entries (RunRunner body, FileEntryBuffer Resource, runner_vtable_from_library helper) as shipped in Slice 5b by revising each entry's prose to record the closure; the four remaining Slice 5a BACKLOG entries (FileEntry enrichment, host_ctx model, Resource<RunSurface>, workspace path Str-to-bytes resolution) stay unchanged.
**Source TOPIC:** `mock/design_rounds/202605250400_topic.viola-254-slice-5b-runrunner-body.md`.

# Doc changes

## CHANGE: doc section `### FileEntryBuffer` ADDED to viola-core DESIGN.md.tmpl
File: `mock/crates/viola-core/DESIGN.md.tmpl`
Verification: `grep -q "^### \`FileEntryBuffer\`" mock/crates/viola-core/DESIGN.md.tmpl`.

New section under the `## Slice 4 file-discovery shapes` heading (alongside DiscoveredFilePaths). Documents the host-shim-populated FFI-shape array Resource: `UnsafeCell<[FileEntry; MAX_DISCOVERED_FILES]>` plus `Cell<arvo::USize>` length, four-invariant Sync contract matching DiscoveredFilePaths, `pub unsafe fn push` host-shim seam with `# Safety` clause, safe `entries_len` and `entries_ptr` accessors. The `entries_ptr` accessor surfaces the raw pointer needed for `RunScope.files`.

## CHANGE: doc section `### runner_vtable_from_library` ADDED to viola-core DESIGN.md.tmpl
File: `mock/crates/viola-core/DESIGN.md.tmpl`
Verification: `grep -q "^### \`runner_vtable_from_library\`" mock/crates/viola-core/DESIGN.md.tmpl`.

New section under the existing `## Vtable dispatch helpers` heading. Documents the new helper that takes `&hilavitkutin_linking::Library` and walks the descriptor's provider table to return `Maybe<&'static RunnerExecuteScopeVtable>`. Returns `Maybe::Isnt` on descriptor missing, descriptor invalid, runner provider not found, or vtable_ptr null. Parallels the Slice 3 `resolve_descriptor` walk pattern. The pre-#254 `runner_vtable(&Extension)` stays until Slice 8 deletes the `Extension` type.

## CHANGE: doc section `### RunRunner Slice 2a revision` REVISED in viola-core DESIGN.md.tmpl to record Slice 5b body
File: `mock/crates/viola-core/DESIGN.md.tmpl`
Verification: `grep -q "Slice 5b body" mock/crates/viola-core/DESIGN.md.tmpl`.

The existing Slice 2a RunRunner subsection (which documents Slice 1 declaration) gets a Slice 5b body paragraph documenting the AccessSet refinement, the runner plugin resolution path, the RunScope marshalling on stack, the FFI call, and the failure path emitting WuDiagnostic. The Slice 5a NAM carrier section already documents the Column<Nam> singleton-row write convention.

## CHANGE: BACKLOG entry `RunRunner body and FFI invocation` REVISED in viola-core/BACKLOG.md.tmpl to mark shipped
File: `mock/crates/viola-core/BACKLOG.md.tmpl`
Verification: `grep -q "Shipped in Slice 5b" mock/crates/viola-core/BACKLOG.md.tmpl`.

The Slice 5a entry's prose revises to record "Shipped in Slice 5b" along with the round timestamp. The discipline pattern is the same as the Slice 3 docs round revising the Slice 2b populate_from_borrowed entry: pre-1.0 entries are consolidated in place rather than duplicated per the no-legacy-shims-pre-1.0 discipline.

## CHANGE: BACKLOG entry `FileEntryBuffer Resource for RunRunner` REVISED in viola-core/BACKLOG.md.tmpl to mark shipped
File: `mock/crates/viola-core/BACKLOG.md.tmpl`
Verification: `grep -q "FileEntryBuffer Resource for RunRunner" mock/crates/viola-core/BACKLOG.md.tmpl && grep -q "Shipped in Slice 5b" mock/crates/viola-core/BACKLOG.md.tmpl`.

Revises to record "Shipped in Slice 5b". The host shim implementation lands in Slice 8 (viola-cli host-shim rewire); the Resource shape itself ships in Slice 5b for the WU to consume.

## CHANGE: BACKLOG entry `runner_vtable_from_library helper for viola-core invoke` REVISED in viola-core/BACKLOG.md.tmpl to mark shipped
File: `mock/crates/viola-core/BACKLOG.md.tmpl`
Verification: `grep -q "runner_vtable_from_library helper" mock/crates/viola-core/BACKLOG.md.tmpl && grep -q "shipped in Slice 5b" mock/crates/viola-core/BACKLOG.md.tmpl`.

Revises to record the helper landed in Slice 5b. The sibling `lint_vtable_from_library` for Slice 6 stays a forward-looking note.

# Verification target

After the round ships: `cargo check` clean. `cargo mock --lint-only --commit` clean. Every backticked Slice 5b type name in DESIGN.md.tmpl exists in source. The three closed BACKLOG entries reflect the shipped status; the four remaining Slice 5a entries are unchanged.
