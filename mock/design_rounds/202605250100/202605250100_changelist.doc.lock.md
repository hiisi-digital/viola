**Date:** 2026-05-25
**Phase:** DOC
**Scope:** Docs-only round closing the Slice 3 audit-trail gap. Lands `### ExtensionHost`, `### PluginEntry` revision, and `### PluginEntryOwned` revision in viola-core / viola-config DESIGN.md.tmpl. Adds two BACKLOG entries (Column::len writer accessor, runtime test-Ctx fixture for LoadPlugins) in viola-core/BACKLOG.md.tmpl. Revises the existing `populate_from_borrowed` entry in viola-config/BACKLOG.md.tmpl to reflect Slice 3's plugins-arm landing.
**Source TOPIC:** `mock/design_rounds/202605250100_topic.viola-254-slice-3-design-sections.md`.

# Doc changes

## CHANGE: doc section `### ExtensionHost` ADDED to viola-core DESIGN.md.tmpl
File: `mock/crates/viola-core/DESIGN.md.tmpl`
Verification: `grep -q "^### \`ExtensionHost\`" mock/crates/viola-core/DESIGN.md.tmpl`.

New section under a `## Slice 3 plugin-host shapes` heading paralleling the Slice 2b column-record family. Documents the singleton Resource shape, the four-invariant `unsafe impl Sync` contract, MaybeUninit-based slot storage, per-run Drop semantics, `library_at` unconditional bounds check (UB-soundness gate for the MaybeUninit::assume_init_ref read), `pub(crate) unsafe fn push` surface.

## CHANGE: doc section `### PluginEntry` REVISED in viola-core DESIGN.md.tmpl
File: `mock/crates/viola-core/DESIGN.md.tmpl`
Verification: `grep -q "host_idx" mock/crates/viola-core/DESIGN.md.tmpl`.

The Slice 1 `### PluginEntry` placeholder section flips to the fields-bearing four-field shape. The fields are rendered as a four-row table: `name: Str`, `roles: crate::role::Mask64`, `abi_version: AbiVersion`, `host_idx: arvo::Cap`. Documents the Copy-element-type requirement that motivated Path B over Path A (`Library` has `Drop`, cannot satisfy `Column::ColumnValue: Copy + 'static`). Documents the host_idx bridge to `Resource<ExtensionHost>`. Names the Debug-derive omission and points at `viola-core/SHAME.md.tmpl` for the discipline tracking; rejects a workspace-local Debug-newtype wrap on its own merit (the wrap would shadow the upstream type in every downstream signature for one synthetic format trait).

## CHANGE: doc section `### PluginEntryOwned` REVISED in viola-config DESIGN.md.tmpl
File: `mock/crates/viola-config/DESIGN.md.tmpl`
Verification: `grep -q "interior mutability" mock/crates/viola-config/DESIGN.md.tmpl`.

The Slice 2a `### PluginEntryOwned` placeholder section flips to the fields-bearing two-field shape: `name: Str`, `path: Str`. Documents the Slice 3c interior-mutability wrap on the `plugins` and `plugins_len` fields (UnsafeCell + Cell), the `unsafe impl Sync for ViolaConfigOwned` four-invariant contract, the `plugin_at` and `plugin_path` accessors that hide the UnsafeCell from consumers.

## CHANGE: BACKLOG entry `Column::len writer-side accessor in hilavitkutin-api` ADDED to viola-core/BACKLOG.md.tmpl
File: `mock/crates/viola-core/BACKLOG.md.tmpl`
Verification: `grep -q "Column::len writer-side accessor" mock/crates/viola-core/BACKLOG.md.tmpl`.

The LoadPlugins body works around the missing writer-side accessor with a local counter (`loaded_count`) that tracks column length by construction. Future hilavitkutin-api revision adds the accessor and Slice 3's local-counter workaround simplifies.

## CHANGE: BACKLOG entry `Runtime test-Ctx fixture for LoadPlugins` ADDED to viola-core/BACKLOG.md.tmpl
File: `mock/crates/viola-core/BACKLOG.md.tmpl`
Verification: `grep -q "Runtime test-Ctx fixture for LoadPlugins" mock/crates/viola-core/BACKLOG.md.tmpl`.

Slice 3 shipped a compile-time AccessSet-shape witness test for LoadPlugins; runtime parse-and-load tests defer to a follow-up that ships the same test-Ctx fixture infrastructure the Slice 2b LoadConfig runtime tests need. Inherits the Slice 2b deferral.

## CHANGE: BACKLOG entry `populate_from_borrowed owned-record fill-in` REVISED in viola-config/BACKLOG.md.tmpl
File: `mock/crates/viola-config/BACKLOG.md.tmpl`
Verification: `grep -q "Slice 3 shipped the plugins arm" mock/crates/viola-config/BACKLOG.md.tmpl`.

The existing Slice 2b entry covered the deferred fill-in for all four arms (plugins, gates, rules, partial_rules). Slice 3c shipped the plugins arm with the UnsafeCell + Cell wrap. The entry revises to record the partial completion: plugins arm shipped, gates / rules / partial_rules arms remain deferred to their producer slices. Pre-1.0; no shim, just one consolidated entry per the no-legacy-shims discipline.

# Verification target

After the round ships: `cargo check` clean across viola-config and viola-core (no source change). `cargo mock --lint-only --commit` clean. Every backticked Slice 3 type name in DESIGN.md.tmpl exists in source. BACKLOG entries are UNBACKTICKED.
