**Date:** 2026-05-25
**Phase:** TOPIC
**Scope:** A docs-only round that closes the audit-trail gap from Slice 3. Lands the missing `### TypeName` sections in `viola-core/DESIGN.md.tmpl` and `viola-config/DESIGN.md.tmpl` for the Slice 3 type additions / revisions (`ExtensionHost`, `PluginEntry` revision, `PluginEntryOwned` revision), plus BACKLOG entries for the items the locked SRC CL named as deferrals.
**Source topics:** `mock/design_rounds/202605250000/` (the closed Slice 3 round; eight PRs merged across the cycle).

# Why this round exists

Round 202605250000 (Slice 3) merged across eight PRs: topic, DOC CL, SRC CL, 3a partial (`PluginEntryOwned` ZST flip + `plugin_path`), 3b viola-core types (`ExtensionHost` + `PluginEntry` flip + AccessSet + witness test + SHAME), 3c plugins array `UnsafeCell` wrap + `populate_from_borrowed` plugins arm, 3d LoadPlugins body + `resolve_descriptor`, lock + close. Source is on dev; the `design-doc-source-mismatch` push-gate currently exits clean because the new types appear inside Slice 2a section blocks via backticked names, but the audit-trail expectation per the Slice 2b precedent is that every shipped type carries a `### TypeName` paragraph naming the shape and the why.

This round is the analogous follow-up for Slice 3. The four locked SRC CL deferrals (host-shim long-lived interner wiring, runtime test-Ctx fixture for LoadPlugins, `hilavitkutin-api` `Column::len` writer-side accessor, gates / rules / partial_rules `populate_from_borrowed` arms) move from the locked SRC CL prose into formal BACKLOG entries so future readers find them in the canonical surface.

# What lands

In `viola-core/DESIGN.md.tmpl`, under a new `## Slice 3 plugin-host shapes` section: `### ExtensionHost` and `### PluginEntry` revision. The field surface is rendered as a four-row table (matching the writing-style rule that tables fit parallel-column data with at least three rows).

In `viola-config/DESIGN.md.tmpl`, the `### PluginEntryOwned` section revises from the Slice 2a ZST description to the two-field shape (`name: Str`, `path: Str`). Adds a paragraph on the `UnsafeCell` / `Cell` interior-mutability wrap on the `plugins` and `plugins_len` fields per Slice 3c, plus `### plugin_at` and `### plugin_path` accessor paragraphs.

In `viola-core/BACKLOG.md.tmpl`, two new entries: `Column::len writer-side accessor in hilavitkutin-api` and `Runtime test-Ctx fixture for LoadPlugins`. Both inherit shape from the Slice 2b LoadConfig fixture entry.

In `viola-config/BACKLOG.md.tmpl`, the existing `populate_from_borrowed owned-record fill-in` entry revises to note that Slice 3 shipped the plugins arm and the gates / rules / partial_rules arms remain deferred to their respective producer slices. Consolidates rather than duplicates per `no-legacy-shims-pre-1.0.md`.

# Out of scope

- Slice 4 (DiscoverFiles body). Opens its own round.
- Touch-ups to viola-core / viola-config source. This is a docs-only round.
- Upstream Debug derives on arvo-bitmask::Mask<W> and hilavitkutin-extensions::AbiVersion. The SHAME entry on viola-core handles the discipline gap for now.
- `Mask64` rename or pub re-export decisions. Slice 3b shipped `pub type Mask64 = Mask<QWord>;` at `viola-core/src/role.rs:26`; the alias is the canonical name and the docs reference it.

# Verification target

After the round ships:

- `cargo check` clean across viola-config and viola-core (no source change should regress).
- `cargo mock --lint-only --commit` clean.
- Every backticked Slice 3 type name in viola-core / viola-config DESIGN.md.tmpl exists in source.
- BACKLOG entries are UNBACKTICKED.
