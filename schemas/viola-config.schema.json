{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://jsr.io/@hiisi/viola/schemas/viola-config.schema.json",
  "title": "Viola Configuration",
  "description": "Configuration schema for viola convention linter in deno.json",
  "type": "object",
  "properties": {
    "plugins": {
      "$ref": "#/$defs/plugins"
    },
    "inherit": {
      "$ref": "#/$defs/inherit"
    },
    "config": {
      "$ref": "#/$defs/linterConfig"
    }
  },
  "additionalProperties": {
    "$ref": "#/$defs/scopeConfig"
  },
  "$defs": {
    "severity": {
      "type": "string",
      "enum": ["error", "warn", "info", "off"],
      "description": "Output severity level for issues"
    },
    "issueCategory": {
      "type": "string",
      "enum": [
        "correctness",
        "maintainability",
        "consistency",
        "performance",
        "style"
      ],
      "description": "Category of an issue - what kind of problem it represents"
    },
    "issueImpact": {
      "type": "string",
      "enum": ["critical", "major", "minor", "trivial"],
      "description": "Impact level of an issue - how urgent it is (critical > major > minor > trivial)"
    },
    "patternValueObject": {
      "type": "object",
      "properties": {
        "severity": {
          "$ref": "#/$defs/severity",
          "description": "Output severity for matched issues"
        },
        "minConfidence": {
          "type": "integer",
          "minimum": 0,
          "maximum": 100,
          "description": "Minimum confidence score (0-100) required to report the issue"
        }
      },
      "required": ["severity"],
      "additionalProperties": false
    },
    "patternValue": {
      "oneOf": [
        { "$ref": "#/$defs/severity" },
        { "$ref": "#/$defs/patternValueObject" }
      ],
      "description": "Severity or configuration object for a pattern"
    },
    "plugins": {
      "type": "array",
      "items": {
        "type": "string",
        "description": "Import specifier for a linter plugin module"
      },
      "description": "List of plugin modules to load. Uses the same syntax as TypeScript/Deno imports: import map references (e.g., '@hiisi/viola-default-lints'), JSR specifiers (e.g., 'jsr:@scope/package'), npm specifiers (e.g., 'npm:package'), or URLs.",
      "examples": [
        ["@hiisi/viola-default-lints"],
        ["jsr:@hiisi/viola-default-lints", "jsr:@someone/custom-linter"],
        ["./local-linter.ts"]
      ]
    },
    "inherit": {
      "type": "array",
      "items": {
        "type": "string",
        "description": "Preset name (short or qualified as plugin/preset)"
      },
      "description": "Preset names to inherit from loaded plugins. Presets are applied in order (later presets override earlier). User's own rules are applied last (always win). Note: 'default' presets from plugins are auto-applied before these explicit inherits.",
      "examples": [
        ["strict"],
        ["viola-default-lints/strict", "my-plugin/relaxed"]
      ]
    },
    "linterConfig": {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "description": "Linter-specific configuration options"
      },
      "description": "Per-linter configuration options. Keys are linter IDs, values are linter-specific config objects. Validated against schemas provided by plugins.",
      "examples": [
        {
          "type-location": {
            "allowedDirs": ["src/types", "packages/*/types"]
          },
          "duplicate-strings": {
            "minLength": 10,
            "threshold": 3
          }
        }
      ]
    },
    "scopeConfig": {
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/patternValue"
      },
      "description": "Issue patterns mapped to severities. Keys are pattern strings matching linter/issue identifiers.",
      "examples": [
        {
          "*>=major": "error",
          "*>=minor": "warn",
          "*=trivial": "off",
          "similar-functions/*": "error",
          "deprecation/stale": { "severity": "error", "minConfidence": 90 }
        }
      ],
      "propertyNames": {
        "description": "Pattern syntax: 'linter/issue' (exact), 'linter/*' (all from linter), '*::category' (by category), '*>=impact' (by impact threshold), or combinations"
      }
    }
  },
  "examples": [
    {
      "plugins": ["@hiisi/viola-default-lints"],
      "inherit": ["strict"],
      "config": {
        "type-location": {
          "allowedDirs": ["src/types"]
        }
      },
      "**/*.ts": {
        "*>=major": "error",
        "*>=minor": "warn",
        "*=trivial": "off",
        "similar-functions/*": "error",
        "deprecation/stale": "error"
      },
      "**/*_test.ts": {
        "*>=major": "warn",
        "deprecation/*": "off"
      }
    }
  ]
}
